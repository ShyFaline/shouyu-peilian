# 整站收口任务与目录交接

日期：2026-09-26。负责人已批准本轮：暂停素材，先跑通现有项目。先读根 AGENTS.md。本文件不代表功能已验收，不改写负责人已确认的真人验收。

## 目录已归拢

唯一桌面入口：`C:\Users\15424\Desktop\2026.9.15`。

三个隔离副本已使用 Git 原生移动归入：
- `工作副本/核心实现`：原 `2026.9.15-wt-core`。
- `工作副本/执行规则`：原 `shouyu-agent-rules-20260926`，本轮目录管理分支。
- `工作副本/K3文书`：原 `shouyu-k3-docs-20260926`。

目录移动不合并 main、不自动部署，不删除既有改动。已交付的11个文件在主工作目录与 f2ed113 对应内容一致（忽略换行格式）；主目录另有未提交页面改动，未被覆盖。核心与K3副本各自原有的无关班服表格删除状态保留，不加入本任务提交。

所有窗口先重新定位目录，停止使用旧路径。后续工作副本只建在主项目的工作副本目录；本地Git排除该目录。不能恢复成所有人并行修改同一份文件。

## 共同执行约定

1. 真人示范、招募、图片处理、Blender重建、整站重设计、扩字母和训练继续暂停。保留成果，不继续扩写素材需求。
2. 每块依次为：总控任务、实现、文书事实、K3独立验证、总控验收。文书无必要代码写不适用。
3. 每个逻辑改动验证后commit并push本席任务分支。明确本地与远端分支，不复用别席分支；不自动合并或推main。
4. 先报工作目录、分支、HEAD、未提交文件与文件所有权。不复制他人的未完成工作。锁冲突先停相应文件，继续无冲突部分。
5. 负责人确认的真人验收按负责人确认记录，不索要材料作为现有工程测试的前置条件，不编造样本，也不自动升格内容状态。
6. 最终交付说清：做成什么、还剩什么、影响使用吗、证据在哪；附命令退出码、提交和实际推送状态。自测不得冒充独立验收。

## Grok：唯一核心修复与整站集成席

任务：固定被测版本，检查启动、资源、主路径/图鉴、权限失败、摄像头启动停止重开、镜像、目标切换、断流、有效/无效快照、离线回放。只修明确阻断。

从移动后的核心副本开始。主目录现有index/styles修改归原作者，先取得具体差异交接，不擅自复制提交。联调基线必须注明包含哪些已交接文件。

先跑核心测试，VM测试使用真正Node及 --experimental-vm-modules。记录process.execPath、Node版本，识别Bun兼容入口；模块加载失败、测试加载器缺陷和产品行为错误分开处理。旧的4过22失败不是22个已确认产品缺陷。

已有图片切换/失败测试先复跑，不能因其已提交就宣布行为已验收。修复需有能捕获旧问题的回归。保持质量/内容/时效门，不降阈值、不改U定义。

给K3文书提供当前事实和可复现步骤，交K3验证窗口独立验收；报告运行环境不足项。只读检查现有线上站点，不部署新版本。

## DS：离线回放与报告修补席

任务：不重写评测系统。修正humanEvidence逐样本条件：来源、合格独立人工标签、协议关联、实际评估结果须在同一合格样本上成立，不能跨样本拼条件。区分已尝试与有可判定结果，字段声明不能当作真实性证明。

补反例：一帧有协议但blocked，另一帧可判定但无协议；camera声明配synthetic-construction标签；零样本和全部阻断。全体与条件指标保持并列。

将迁移测试的“哈希是字符串”改成实际前后比较。保留历史产物；旧bili调用器若不兼容先明确拒绝并给可执行说明，不重跑视频/渲染生产线、不扩数据任务。

写锁tools/assessment及已明确移交的旧离线脚本，核心接口请求交Grok。独立worktree放工作副本目录，从约定基线开始。回归命令verify-skeleton与verify-legacy-migration。交K3独立复验，给文书准确统计定义和限制。

## K3文书：纠错并交一页老板进度

本轮停止新增素材卡/招募要求，现有材料仅存档。更正文书中无依据的“AGENTS已裁定U四指”：当前没有该裁定，核对来源后撤回失实归因，保留更正记录，不擅改规则。

修正不存在的候选目录、章节和无出处数字；核对后引用实际8张候选位置。现阶段不验图、不要求追加建模。将当前执行身份统一为K3独立验证窗口，历史记录保留当时身份。

测试数字以对应版本最新结果为准；保留旧结果日期，不能抄历史自测当当前通过。负责人确认与代理实测分栏。整理一页验收表：功能、结果、负责人、证据、是否阻断使用。不得把“未提交/未推送”等历史句当现状。

写锁docs文书及项目情况简述，排除代理合同和独立报告。向总控交付需裁定项，不为文书完整阻塞工程联调。

## K3验证：独立完整走通用户流程

与K3文书分窗，产品源码和文书只读。首先明确被测版本及变更清单，测试时冻结；途中变更则受影响项失效并重测。

验证本地与现有线上入口：资源加载、主路径与图鉴、默认U不正式通过、目标切换、镜像开关、摄像头权限失败/启停恢复、断流清理、有效新鲜快照下载、无手/多手/过期/切目标后不可导出、离线回放与来源页。断网测试保留本地服务，注明实际如何隔离外网。无法执行的项写未执行，不推定通过。

复跑核心、真正Node VM、评测与迁移测试。浏览器模拟不是真人；负责人此前确认不等于你本次执行。独立构造DS所列反例，不只跑作者测试。文书的U裁定与证据路径问题须核对真实文件。

仅允许在工作副本中的独立报告目录记录必要的脱敏证据；不修改被测实现、不改golden、不包含真人照片或私密样本。报告本身需按AGENTS独立提交推送；纯只读无文件交付不造空提交。

交总控：通过、退回、环境阻塞三类；逐项说明影响，禁止自己宣布上线或全项目验收通过。

## Gemini：停止素材与页面扩展，交接既有差异

停止真人素材、建模和重设计。先报告停止状态；保留原成果，不删除不回退。列清现有index/styles修改的目的、具体差异、是否影响原页面，交Grok决定纳入联调版本。

不把其他人的修改打包提交。仅当本席既有逻辑改动已验证且能安全隔离时提交到自己的任务分支；否则如实交补丁范围，待集成席串行处理。暂停的新素材候选不因旧prompt要求而继续开发或公开上传。没有必要新改动不造提交。

## 总控最终门槛

收到实现、文书及独立报告后，按同一版本判定。只在关键使用流程无阻断、限制表达真实时批准集成；main合并与部署仍须负责人明确授权。素材质量不作为本轮新增开发前置条件，教学正式能力不因工程通过自动升格。
