# 手语学习陪练 · 技术栈前期规划

> 本文把「开源模型考察」收成一套**第一期可以开工、层与层可以整层替换**的技术选择。  
> 不是施工图，也不是最终规格。任何一层都可以被更好的方案换掉；换的时候只改该层实现和适配器，不推翻产品。  
> 依据：`手语陪练-项目日志与框架.md`（2026-09 草案）与同期开源考察结论。

**文档状态：** 前期规划（Proposed）  
**日期：** 2026-09-15  
**规划范围：** 比赛演示前的感知 → 理解 → 最小体验；不含 App、小程序、连续语句翻译。

---

## 1. 一句话

第一期做 **浏览器本地跑的国标手指字母陪练**：摄像头只变成手的几何，几何再变成「像不像、差在哪」；**不训检测网，不接翻译大模型，默认不上传视频。**

---

## 2. 需求摘要

### 2.1 产品要什么

| 要 | 不要 |
|---|---|
| 对着练、错了能被指出来 | 手语翻译器 / 手语 → 整句文本 |
| 国标手指字母（静态 / 准静态） | 第一期就覆盖词、句、方言 |
| 评委能当场比、当场看反馈 | 只交提示词或无法运行的 skill |
| 关键点可替换、来源可引用 | 自训一个「超过别人」的检测网 |

最小能跑的线（与框架文一致，本文把它落成技术验收）：

1. 打开摄像头，画出一只手的骨架  
2. 库里至少两个目标手型  
3. 比一个，给出对/不对，或再细一点（哪根手指）  
4. 换人、换光，再试一次  

### 2.2 非功能（按比赛演示，不按互联网产品）

| 类别 | 第一期目标 | 说明 |
|---|---|---|
| 运行位置 | 本机浏览器，识别在设备上完成 | 现场网络失败时仍能练 |
| 延迟 | 骨架与反馈目视跟手（约 15–30 FPS） | 不追求云端高精度 |
| 隐私 | 视频默认不离开本机 | 采集若发生，须知情、可关、可删 |
| 兼容 | 现场主路径：桌面 Chrome | 手机 / Safari 作为加分，不是门槛 |
| 规模 | 单人摄像头，偶尔双人围观 | 不做并发、不做账号体系 |
| 可维护 | 四人能在一周内说清「改哪一层」 | 依赖少、接口窄 |
| 安全 | 无用户账户；摄像头权限按浏览器授权 | 不做诊断、不声称「学会手语」 |
| 许可 | 只用可引用的开源与公开国标 | 单独开一页来源 |

---

## 3. 约束与非目标

**约束**

- 感知输出必须是中性几何（点、线、置信度、左右手、是否出框），这一层禁止写死「这是字母 M」。
- 理解层的解释必须让人能改动作，禁止只丢一个分数。
- 内容以公开可引用的规范为准，第一期对齐 **GF 0021—2019《汉语手指字母方案》**。
- 识别模型可以没有现成国标权重——考察已确认：**没有可直接下载的国标手指字母开源识别器。** 第一期自建规则，必要再采关键点。

**明确非目标（第一期不做）**

- 连续手语翻译、500 词孤立词识别、Uni-Sign / CSL-Daily 一类大模型  
- 自训手部检测网络、纯图像 CNN 字母分类（无法逐指解释）  
- 把 ASL 字母预训练头直接当国标识别器  
- 3D 虚拟人、小程序 / App 上架、账号与云端存视频  
- 为「以后可能有一万用户」准备微服务、数据库、GPU 推理服务  

---

## 4. 总体结构

沿用已有四层。技术栈只是这四层的第一期填法。层间只传下面这些窄接口。

```
                    浏览器（单页，本地静态资源）
    ┌─────────────────────────────────────────────────────────┐
    │ 体验层  Vite + TypeScript + DOM/Canvas                  │
    │         课 / 练 / 对照 / 一句人话反馈                     │
    └──────────────────────────┬──────────────────────────────┘
                               │ PracticeEvent
                               │ { letterId, pass, issues[], score? }
    ┌──────────────────────────▼──────────────────────────────┐
    │ 理解层  纯函数：规则几何为主，小分类器为辅（可空）         │
    └──────────────────────────┬──────────────────────────────┘
                               │ HandFrame
                               │ { t, handedness, landmarks[21],
                               │   worldLandmarks?, conf, inFrame }
    ┌──────────────────────────▼──────────────────────────────┐
    │ 感知层  MediaPipe Tasks Hand Landmarker（可整层替换）     │
    └─────────────────────────────────────────────────────────┘
                               内容层：JSON 字母包 + 国标引用
                               （示范图 / SignPinyin / 规则卡）
```

**为何是浏览器单页，而不是 Python 窗体或前后端：**  
框架已规定第一期浏览器能跑即可；摄像头、画骨架、给反馈都不需要服务器。少一个进程，现场少一种失败。Python 只留给离线实验，不走演示主路径。

---

## 5. 层间接口（写死形状，不写死实现）

这些类型是合同。感知换成 RTMPose、理解改成近邻检索，只要还吐这些字段，体验层不用重写。

```ts
/** 感知层 → 理解层。坐标约定：图像像素或 0–1 归一化，项目内只选一种并在适配器里统一。 */
export type Landmark = { x: number; y: number; z?: number; name?: string };

export type HandFrame = {
  t: number;                    // performance.now()
  handedness: "Left" | "Right" | "Unknown";
  landmarks: Landmark[];        // 长度 21，MediaPipe 拓扑
  worldLandmarks?: Landmark[];  // 可选，米制，便于以后做 3D 夹角
  conf: number;                 // 0–1
  inFrame: boolean;
};

export type FingerId = "thumb" | "index" | "middle" | "ring" | "pinky";

export type Issue = {
  finger?: FingerId;
  code: string;                 // 如 "index.not_extended"
  hint: string;                 // 给人看的一句
};

export type PracticeEvent = {
  letterId: string;             // 内容层 ID，如 "GF0021.B"
  pass: boolean;
  issues: Issue[];
  score?: number;               // 可选；禁止作为唯一反馈
  strictness: "intro" | "gate";
};
```

内容层字母包（示例形状）：

```json
{
  "id": "GF0021.B",
  "standard": "GF 0021-2019",
  "label": "B",
  "demo": { "type": "svg", "src": "/content/letters/B.svg" },
  "rules": {
    "fingers": {
      "thumb": { "curl": "half_or_full" },
      "index": { "curl": "none" },
      "middle": { "curl": "none" },
      "ring": { "curl": "none" },
      "pinky": { "curl": "none" }
    }
  }
}
```

规则字段第一期允许不完美；**有两个真字母、能分对错**即可。争议手型记在内容包的 `notes` 里，不在代码里发明「我们队的手语」。

---

## 6. 分阶段（技术只服务当前阶段）

| 阶段 | 目标 | 技术上必须有 | 技术上禁止同时加 |
|---|---|---|---|
| **P0 看见手** | 摄像头 + 21 点骨架 | Hand Landmarker、本地模型文件、镜像开关 | 分类器、关卡、词表 |
| **P1 两个手型** | 一对易混字母，规则判定 + 一句解释 | 几何规则、字母 JSON、对/错 + 逐指 hint | 训练管线、后端 |
| **P2 能练** | 换目标、换严格度、无摄像头先看示范 | 内容包 ≥ 少量字母、对照分屏 | 虚拟人、账号 |
| **P3 才考虑** | 轨迹词、双手、小分类器第二票 | 与 P0–P2 同一套 `HandFrame` | 翻译模型 |

P0 不通，停在感知层。不要用「先把 React 路由搭完」代替画骨架。

---

## 7. 各层选型

### 7.1 交付与工程

| 选择 | 第一期 | 备选 | 不选及原因 |
|---|---|---|---|
| 形态 | 静态单页，可 `vite` 本地打开 | 以后再包成桌面 / 小程序 | Electron / 小程序：现场多一种安装失败 |
| 语言 | TypeScript | JS | 层间接口需要类型，避免四人口头对字段 |
| 构建 | Vite | — | 快、静态资源（wasm / `.task`）好放 `public/` |
| UI | 原生 DOM + Canvas | 体验层以后可套 React | 识别循环每帧 `setState` 会卡；若用 React，**每帧结果不得进 React state** |
| 包管理 | npm 或 pnpm，锁文件进仓库 | — | 现场 `npm ci` 可复现 |
| 后端 | **无** | 以后若要排行榜再加静态托管 | Flask/Node 推理服务：与「视频不上传」冲突，现场还依赖端口 |
| 质量 | 几何函数用 Vitest 测夹具点 | — | 摄像头画面无法当 CI 主断言 |
| 部署 | `dist/` 本地演示；可选 GitHub Pages | — | 模型文件较大时 Pages 要注意体积，现场以 U 盘 / 本地为准 |

建议目录（未建仓库前的约定，可改名、不要拆层）：

```
/
  public/
    models/          HandLandmarker .task 与 wasm（现场不靠外网）
    content/         字母示范 svg / 图
  src/
    perception/      摄像头、Landmarker、HandFrame 适配器
    understanding/   几何、规则匹配、PracticeEvent
    experience/      页面、画骨架、文案
    content/         字母包加载
  tests/
    understanding/   用录好的 landmarks JSON，不打开摄像头
```

### 7.2 感知层

| 项 | 决定 |
|---|---|
| 主方案 | **MediaPipe Tasks Vision `HandLandmarker`**（`@mediapipe/tasks-vision`） |
| 模型 | 官方 `hand_landmarker.task`（float16，约 8 MB），**下载进 `public/models`** |
| 许可 | Apache-2.0，来源页必须写 |
| 运行 | `VIDEO` 模式 + `requestAnimationFrame`；优先 GPU delegate，失败回退 CPU |
| 手数 | 第一期按 1 只手做产品逻辑；检测可开到 2，避免第二只手误判时要能提示「请只伸一只手」 |
| 坐标系 | 适配器里统一：镜像后的归一化坐标（前置摄像头与用户动作同向） |
| 替换位 | `perception/createTracker.ts` 只暴露 `subscribe(frame => …)`；换成 RTMPose/ONNX 时只改这一处 |

**为何不是这些**

- **RTMPose / RTMW：** 关键点可能更稳，但浏览器要 ONNX + WebGPU，体积和工程量都大。静态字母用不上全身点。作为 P3「关键点不稳再换」的备胎。
- **旧 `@mediapipe/hands` / TF.js `hand-pose-detection`：** 同为 21 点，但 Tasks 是当前维护路径。
- **自训检测、YOLO 手部框：** 违反「不把检测网当目标」；也没有逐指几何。
- **MediaPipe Holistic / Pose：** 留给以后带轨迹的词，第一期会增加半身入画失败。

现场风险：CDN 被墙或断网。因此 wasm 与 `.task` **必须能离线加载**。开发期可以用 CDN，演示包必须是本地文件。

### 7.3 理解层

这是产品可能拉开距离的一层，也是最不该绑死某个仓库权重的一层。

| 路径 | 第一期角色 | 技术 |
|---|---|---|
| **主：规则几何** | 判定 + 解释 | 自写（思路可参考 [fingerpose](https://github.com/andypotato/fingerpose)：每指弯曲 / 朝向 / 张角） |
| 辅：小分类器 | 可空。P1 不做 | 若 P2 以后要「更像哪一个字母」，用关键点 MLP/RF（流程可参考 [Kazuhito00](https://github.com/Kazuhito00/hand-gesture-recognition-using-mediapipe)），输入仍是 `HandFrame`，**禁止改吃原图** |
| 不采用 | — | Gesture Recognizer 内置 7 手势；ASL 字母头；图像 CNN |

规则引擎建议输出：

- `pass`：是否达到当前严格度  
- `issues[]`：最多 1～2 条人话（入门阶段一条就够）  
- 不要在 UI 默认展示百分制  

几何要测的，用**录下来的 landmarks 夹具**，而不是每次打开摄像头。这样换人实验失败时，能区分「感知抖」还是「规则写错」。

### 7.4 体验层

第一期只做框架里「跟练 + 对照」的最小集。

| 能力 | 做法 |
|---|---|
| 开摄像头 | `getUserMedia`；失败时走无摄像头示范（看、模仿，不评分） |
| 画骨架 | 覆盖在 `<video>` 上的 `<canvas>`，用 Tasks 的 `DrawingUtils` 或自绘 |
| 对照 | 左示范（**Blender 渲染的标准手 PNG**；SignPinyin 仅作角标字形），右自己 |
| 反馈 | 大字：到位 / 再试试；下面一行 `issue.hint` |
| 换字母 | 主路径 8 个（A B U V L Y I W）；其余进图鉴 |
| 镜像 | 默认开；设置里可关（现场左右手问题的第一开关） |

不做：路由很深的 SPA、设计系统、动画引擎、**实时跟随的虚拟人**。离线 3D 标准手渲染是内容层必做。

### 7.5 内容层

| 项 | 决定 |
|---|---|
| 标准 | GF 0021—2019（26 字母 + zh/ch/sh/ng + ê/ü）。JSON 可保留 32 个；**主路径只练 8 个**，判定质量以 U/V 易混对为准 |
| 示范可视化 | **主：Blender 绑定手 + pose library 渲染 PNG**（内容工厂，不进识别循环）。辅：SignPinyin（SIL OFL 1.1）作字形角标，**须注明字体不是识别模型、3D 也不是识别模型** |
| 数据 | 不依赖 Jiang 2020 / Roboflow / 500-CSL。需要数据时：**浏览器录 `HandFrame` JSON**（多人、多光、多距离），记下拍摄条件 |
| 词表生长 | 字母包是 JSON，加一个文件不等于改模型 |

500-CSL、CSL-Daily、Uni-Sign、yangisyong 六词 LSTM：考察结论里属于「翻译 / 词汇」路线，**不进第一期依赖**。若以后做词，仍应走「关键点序列 + 新理解模块」，而不是把翻译模型嵌进陪练。

---

## 8. 关键决策（ADR）

下列决策第一期视为 Accepted；推翻时改本文并在日志里写原因。

### ADR-001 演示形态用浏览器静态页，第一期无后端

- **背景：** 要可运行产品；摄像头、骨架、对错都不需要存用户视频。  
- **决定：** Vite 静态应用；识别全在浏览器。  
- **正：** 现场少端口、少跨域、默认不上传；和「评委能参与」一致。  
- **负：** 不能做云端大模型；进度/错题若要跨设备，以后再加。  
- **否决：** Python + OpenCV 窗体（不像网页产品、分发差）；Flask 推理（视频出本机）。

### ADR-002 感知用 MediaPipe Hand Landmarker，不自训检测

- **背景：** 开源里没有现成国标字母识别器；项目也不和别人比检测网。  
- **决定：** Tasks `HandLandmarker`，模型文件进仓库或演示包。  
- **正：** Apache-2.0、浏览器、21 点、可替换。  
- **负：** 侧光、遮挡、部分机型会抖或丢手；这是实验要回答的问题，不是再训一个检测能立刻消灭的。  
- **否决：** RTMPose（第一期过重）；YOLO/CNN 像素分类（无逐指解释）。

### ADR-003 理解层以规则几何为主，分类器可选且不得替代解释

- **背景：** 要「哪根手指还没伸直」；国标权重不存在。  
- **决定：** fingerpose 式弯曲/朝向/张角 + 字母规则卡；分类器 P1 不做。  
- **正：** 可解释、可单测、可按国标改一条规则而不重训。  
- **负：** 相似手型规则难写；这正是「选一对易混手型做实验」的原因。  
- **否决：** 只输出 87 分；ASL 权重直接当国标；Gesture Recognizer 七手势。

### ADR-004 内容对齐 GF 0021—2019，不把 ASL 当国标

- **背景：** ASL 开源字母模型很多，国标手指字母几乎没有。  
- **决定：** 字母 ID、示范、规则只引用国标；ASL 仓库只许当工程参考（采集 CSV、归一化），不许当内容。  
- **正：** 评委问「教的是哪一套」答得出来。  
- **负：** 要自己写规则、自己采失败案例。  
- **否决：** 用 Kaggle ASL 图集冒充国标数据。

### ADR-005 视频默认不上传，采集与演示分离

- **背景：** 听障家庭 / 现场演示；伦理上不做偷录。  
- **决定：** 主路径无网络请求带图像。若做实验采集，单独开「采集模式」，明示、可关、数据留本机。  
- **正：** 和框架伦理一致。  
- **负：** 不能用云端 API 提高识别。这是接受的。

### ADR-006 Blender 是内容工厂，不是运行时（2026-09-17 新增）

- **背景：** 初版示范是指语字体，初学者无法从字形反推手指；队内希望用 Blender。旧规划「第一期不做虚拟人」若理解成「不准做 3D」，会把示范洞留下。  
- **决定：** 用 Blender 制作 GF 0021 标准手姿态并渲染进练习页。识别仍走 MediaPipe → 规则几何。不把 Blender / Three 虚拟人跟手作为预选范围。  
- **正：** 示范可引用、可复现、评委问「教的是哪只手」答得出来；与感知层解耦。  
- **负：** 要有人在 Blender 里摆手；丑手也比字体强，但必须对照国标图。  
- **否决：** 实时骨骼驱动虚拟人；用 ASL 手模型冒充国标；把渲染图当成识别精度。

---

## 9. 依赖清单（第一期允许出现在 package.json 里的）

**运行时**

- `typescript`  
- `vite`  
- `@mediapipe/tasks-vision`（钉具体版本，写入锁文件）  

**开发**

- `vitest`  
- 可选：`eslint`  

**禁止无理由加入的**

- TensorFlow.js、ONNX Runtime Web、React、路由库、UI 组件库、状态库、后端 SDK、统计 SDK  
- 任何手语翻译仓库作为 npm 依赖  

需要新依赖时：先问「它属于哪一层、能否用现有接口替换」。

---

## 10. 来源与许可（比赛单独一页，此处先列）

| 用途 | 名称 | 许可 | 备注 |
|---|---|---|---|
| 手部关键点 | MediaPipe Hand Landmarker / google-ai-edge | Apache-2.0 | 感知层 |
| 规则思路（非必须拷代码） | andypotato/fingerpose | 按其仓库许可证引用 | 理解层思路 |
| 若以后训小分类器 | Kazuhito00 关键点 MLP 流程 | Apache-2.0 | 只借流程 |
| 内容标准 | GF 0021—2019 | 国家语言文字规范 | 必须引用，不改手型充新 |
| 示范字体（若用） | SignPinyin | SIL OFL 1.1 | 字体 ≠ 识别模型 |
| 自录关键点 / 示范 | 本队 | 拍摄条件写入日志 | 经同意、不外传 |

旧课设、ASL Demo、翻译模型：**不拿来充新。** 抄了交互也要在来源页写。

---

## 11. 风险与缓解（只列技术栈相关）

| 风险 | 为何会发生 | 第一期怎么处理 |
|---|---|---|
| 现场断网，模型从 Google 存储加载失败 | Tasks 默认 CDN | `.task` + wasm 放 `public/models`，演示前断网演练一次 |
| 浏览器不给摄像头 | 权限、HTTP 非 localhost | 用 localhost / HTTPS；准备无摄像头示范模式 |
| 前置镜像导致左右手反了 | MediaPipe 对手性与镜像假设 | 默认镜像；UI 标明「请用右手」；规则按镜像后坐标写 |
| 相似手型分不开 | 国标里本就有接近外形 | P1 只选一对；失败就记案例，收紧范围，不立刻上大模型 |
| 关键点抖动导致规则闪烁 | 单帧判定 | 短时平滑（连续 N 帧通过才算过）；解释仍用当前帧几何 |
| MediaPipe 丢手 | 遮挡、出框、侧光 | `inFrame` / `conf` 不足时说「把手放进框里」，不要乱打分 |
| 四人对字段理解不一致 | 没有类型 | `HandFrame` / `PracticeEvent` 放一个文件，禁止各写各的 |
| 把时间花在翻译 SOTA 上 | 开源检索很容易被大仓库吸走 | 本文 ADR-003/004；周报只看 P0–P1 是否通 |

实验仍然只验证一件事：**机器能不能稳定分开两只很像的手。** 技术栈为这件事服务，不为「仓库 star 数」服务。

---

## 12. P0 / P1 验收（技术角度）

**P0 通过**

- 桌面 Chrome 打开页面，授权摄像头后能看到自己的手和 21 点骨架。  
- 断开来源站网络后刷新，骨架仍在（模型已本地化）。  
- 控制台无循环报错；手出画时骨架消失或提示出框。  

**P1 通过**

- 内容包里两个国标字母，能切换目标。  
- 比目标手型时出现「到位」或一条可执行 hint（例如「食指再伸直」）。  
- 至少换一个人或一种光照再试；结果写入项目日志（成功或失败都算数）。  
- 理解层至少有一条 Vitest：用夹具点断言会发出预期 `Issue.code`。  

未通过时：修当前层，不开启关卡、词表、分类器。

---

## 13. 和开源考察的对照（避免选型回潮）

| 考察对象 | 规划中的位置 |
|---|---|
| MediaPipe Hand Landmarker | **采用**（感知） |
| fingerpose 规则思路 | **采用思路**（理解） |
| Kazuhito00 关键点小模型 | **P2 以后可选**，P1 不依赖 |
| SignPinyin / GF 0021—2019 | **采用**（内容） |
| SignBridge 一类教学 Demo | 只可借鉴交互，不进依赖 |
| yangisyong 六词 LSTM、harveyfly 500-CSL、Uni-Sign、OpenHands | **不进第一期**；属于翻译 / 词汇路线 |
| ASL 字母 CNN / 官方 Gesture 七手势 | **不采用** |

---

## 14. 建议的下一笔（做完就写回项目日志）

1. 建 Vite + TS 空应用，把 `HandLandmarker` 跑通并画出骨架（P0）。  
2. 从国标里定一对易混字母，写成第一条规则卡（P1 内容）。  
3. 来源页草稿：MediaPipe、国标、（若用）SignPinyin。  

不必等本规划写满再动手。P0 的失败比多选一个 npm 包更有信息量。
