# 给 Grok（核心实现席）的变更请求

> 来自文书席 k3，2026-09-24。**全部是请求，不是指令**；`types.js`、`letters.json`、核心判定接口的写锁在 Grok。
> 证据一律指向 [GF0021-2019检索记录](GF0021-2019检索记录.md) 与 [内容核定表](内容核定表.md) 2026-09-24 段。
> 本轮总控裁定：内容证据进入前不改内容相关规则；**先修可靠性**。因此本请求分「内容映射（等内容核定与人工复核后执行）」与「状态门行为确认（即可查）」两类。

## R1. 状态门事实确认（请回执现状，无需改码）

请用当前工作区代码回答（附文件行号）：

1. 页面默认目标 `practice/app.js` 仍为 `GF0021.U`，U 的 `practiceStatus = pending_review`（letters.json）。**页面在 pending_review 目标下的判定输出语义是什么？**（是否可能出现「到位」类正向正式通过；是否已有阻断/示意文案）。这决定「默认目标 = 未裁定字母」是否构成行为规范缺陷，以及最小修复是改默认目标还是加状态门文案。
2. `letters.json` 顶层 `notes` 与 mainPath 的运行时可见性：来源页/图鉴是否有「8 字母是候选池不是已核定」的展示位。
3. 内容状态分层的当前类型支持：`practiceStatus` 现值域是 4 态。总控要求区分三种证据（内容已核定 / 规则覆盖完整 / 真人评估通过）。请评估：是在现有 4 态上加正交字段，还是扩枚举——给接口建议，**不要自行改**。

## R2. 内容映射变更请求（等内容核定/人工复核完成后才执行，先登记）

以下差异源自 GF 0021—2019 原文 OCR（全部条目见 内容核定表 A2/A4）。**在人工对照原图与该字母被核定为正式目标之前，请保持现状，不要改 rules、不要改 how、不要升 accepted。**

| id | 现状（how/rules） | 原文文字（OCR，页码） | 冲突点 | 处置建议 |
|---|---|---|---|---|
| U | 食中二指并拢伸直 | 食、中、无名、小四指并拢直立（页 5） | 二指 vs 四指 | **挂起**，待领域复核；复核前任何改动都禁止 |
| Z | 食指伸直其余收起 | 食、小指横伸指尖朝左（页 5） | 手指集合不符 | 挂起复核；核定前不开正式判定 |
| X/R | X=食指钩；R=食中交叉 | X=食中交叉；R=拇食指张开食指朝左（页 5/4） | 交叉归属疑似写反 | 挂起复核 |
| NG | 中指伸直 | 小指横伸（页 6） | 手指不符 | 挂起复核 |
| CH | 拇食指中指伸出「2019 年后手形」 | 拇指在下四指并拢在上成扁 O（页 6，2019 调整后） | 新旧手形混淆 | 挂起复核 |
| SH | 拇指食指伸直夹角小于 L | 拇指贴近手掌，食中指并拢微曲与手掌成☐度角（页 6，数字未识别） | 形态不符 + 数字缺 | 人工读原图补数字后复核 |
| ZH | 食中无名并拢伸直 | 食中指并拢（+小指横伸？OCR疑）横伸（页 5–6） | 手指集合疑不符 | 人工对图后复核 |
| H | 食中并拢伸直，手**侧放** | 食中指并拢**直立**，掌心向前偏左（页 3） | 朝向不符 | 挂起复核 |
| G | 拇食指伸出，手侧向 | 食指横伸，拇指搭在中指中节指上（页 3） | 拇指处置不符 | 挂起复核 |
| E/ê | E/ê 同形静态描述 | ê=用 E 指式**手上下晃动两下**（页 6） | 动态成分缺失 | demo_only 维持；文案补「ê 在规范中带晃动」，不写可判定 |
| ü | 食中并拢拇指伸出 | 用 U 指式食中无名小指前后晃动 | 形态不符 | pending；依赖 U 裁定 |
| I/J | 小指伸直 | OCR 读「食指」直立/弯曲（页 3，OCR疑） | 食/小疑读 | 优先人工对图；若原文确是食指，how/rules 需改 |
| A/L | 掌向/指向规则未写 | A 手背向右（页 2）；L 掌心向前偏左（页 3） | 缺失非冲突 | 核定后可将掌向写入 how/notes，不进 rules 除非原型支持 |

## R3. 文书依赖（给 K3 回填用）

1. 请把 P1 修复后的实际改动事实、接口合同、参数 UNVERIFIED 清单发回文书席（总控 Prompt 2/3 第 3 条）；说明书「设计过程」只写已发生事实。
2. 反馈文案与状态的现状口径（到位/还不到位/没看到手/未知）请确认文字，说明书引用时保持一致。
3. 6 帧与 400ms 保持 `UNVERIFIED`，说明书将写「参数未验证」；若你调参请先报变更原因（总控 P1 第 3 条）。

## R4. 不建议本轮做的事（与总控一致）

- 不改 U 定义与阈值；不升任何 `accepted_practice`；不把枚举/字段改名当内容核定。
- 不把「默认目标改成已核定字母」当作内容替代方案——默认目标调整需总控定候选名单后再动；R1 先给事实。
