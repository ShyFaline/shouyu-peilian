# 国标手指字母陪练 · 项目情况简述

**项目名称：** 国标手指字母陪练  
**仓库：** https://github.com/ShyFaline/shouyu-peilian  
**比赛：** 第二届「新致杯」AI + 无限万象  
**日期：** 2026-09-20  
**队长姓名：**  
**队员：**

本文只说明现在做成了什么、还没做成什么。不是使用说明书，也不是 AI Coding 过程全文（过程见 `docs/AI-CODING过程.md`）。报名可以提及，不是验收通过，也不是陪练完成。

---

## 一句话

做一个能对着镜头练**国标手指字母**的本地网页：左边给标准手，右边是你自己；比错了会说哪根手指该怎么改。它是**陪练**，不是手语翻译器。

---

## 要解决什么问题

听障家庭的听力家长、窗口工作人员、手语社新社员，学手指字母时往往不缺示范图，缺的是**练的时候有没有人看着、错了能不能指出来**。开源里 ASL 检测和「手语→文字」课设很多，按 GF 0021—2019 做成可练习产品的很少。

我们不声称能让人「学会手语」，也不替代老师。第一期只练手指字母这一层。

---

## 现在已经能跑的

桌面 Chrome 打开即可练（不要用 `file://`）：

```bash
python -m http.server 8765
```

http://127.0.0.1:8765/practice/index.html

- 摄像头在本机识别，视频默认不上传；模型文件已放进仓库，断网刷新仍应能加载。
- 左边示范、右边自己的手和 21 点骨架；反馈只有「到位 / 还不到位」和一句提示，不显示分数。
- 主路径 8 个字母：**A B U V L Y I W**，打开页面默认练 **U**。其余国标字母在可折叠图鉴里；M / N / S、E / ê 标了「易混」，不假装已经分得开。
- 判定是几何规则，不是再训一个检测网。`bun practice/src/evaluate.test.js` **12 passed**（11 条几何 + 第 12 条 fixtures 空目录），用例名以文件为准：U 并拢通过；V 并拢失败并发出手指分开类 hint；缺手时不误判到位；V 真正分开应通过；U 明显分开应失败并出现并拢类 hint；A 正例通过；B 正例通过；L 正例通过；Y 正例通过；I 正例通过；W 正例通过；fixtures 目录可空；若有 HandFrame JSON 则读取并调用 evaluate。这是合成点，不是真人实验，也不报准确率。
- 示范优先用 Blender 渲的标准手图（`demos/` 里 8 张 `GF0021.*_front.png` **已在**）；没有图时回退指语字体。字体和 3D 都**不是**识别模型。图已存在但**非精修**，不写成验收通过。
- 练习页已有本机导出 HandFrame JSON（`practice/app.js`）：默认关闭，勾选后只在本机下载，不上传。
- 内容对齐 GF 0021—2019。J、Z 带轨迹，第一期只核静态手型。

演示路径：开摄像头 → 比 U → 左标准手 → hint 或到位 → 切 V。不另写主持稿。

---

## 怎么用 AI Coding 做的

赛制要求注明工具和用途。报名可以提及，不是验收通过。

作品用 AI Coding 从 0 做，不是旧课设换皮。工具与用途：Grok 4.6 规划（另开一窗）、Grok 4.6 技术、Grok 4.6 文书、Blender 出图。工人编制只有后三个。验收只读，零写入。Blender 只渲示范图，不进识别循环。文书不写口播、不写主持稿。

人拍板、AI 建议被否掉的几条：

1. 不做手语翻译器。  
2. 不自训手部检测网，用本机 MediaPipe Hand Landmarker。  
3. 预选不做实时虚拟人跟着你的手晃。  
4. 不把 32 个字母一次铺开硬练，主路径只留 8 个。

完整过程单独写在 `docs/AI-CODING过程.md`，不埋进说明书。引用见练习页「来源与许可」：MediaPipe（Apache-2.0）、国标 GF 0021—2019、SignPinyin 字体（SIL OFL 1.1）、fingerpose 仅作规则思路。

---

## 还没做、不会写进「已经完成」的

- 真人 U / V 实验未做（空表：`docs/试用户记录.md`）。  
- 8 张图已在 `demos/`，手仍是草稿、非精修，对照国标再调。  
- 不做连续手语句子翻译、不做 App / 小程序、不上传用户视频。

---

## 请组织方怎么理解本项目

这是教育向的可运行网页，范围只陪练国标手指字母。产品指的是陪练本身：开摄像头 → 比 U → 左标准手 → hint 或到位 → 切 V。可交预选草稿、报名截止，都不是产品完成，也不是验收通过。未完成项见上一节，不拿截止日期充完成。

---

## 怎么做出来的

规划另开一窗。工人编制三个：Grok **技术**、Grok **文书**、**Blender** 出图。验收只读，零写入。文书不写口播、不写主持稿。
