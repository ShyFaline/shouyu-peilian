# 手语陪练：总控裁定与多模型执行 Prompt

日期：2026-09-24。性质：当前执行规划，不是完成报告。

本文件修订《总体目标与并行拆分.md》及 09-22 外部反馈的执行安排；历史日志与原 DOCX 保留，不改写过去。发生执行冲突时按本文件处理；事实必须重新核对当前磁盘，比赛规则最终以赛方原始通知为准。

## 1. 总控结论与对旧指令的批判

目标：做一个本地国标手指字母陪练，让初学者看示范、尝试、理解一条纠错、再次尝试。不做翻译器，不把检测关键点包装成自研国标识别网络。

1. **八字母是候选池，不是本轮全部正式开放的硬指标。** 候选仍为 A/B/U/V/L/Y/I/W；先选 2–3 个规范证据充分、示范清楚、规则覆盖可解释的静态目标完成闭环。具体名单由证据决定，不预定 V/A/B 必然正确。U 争议未裁定前不升格。其余保留明确标注的研究/图鉴状态；未经核定的图也不能叫“标准示范”。
2. **不机械重做 A01–A09。** 先核查现有实现，补仍存在的缺陷。内容核定与不依赖手型定义的可靠性修复并行；只有内容相关规则修改、正式开放和真人正确性评估必须等内容证据。
3. **不把全部关口都交给人。** AI 可以做规范检索、出处整理、回放器、单元测试、浏览器流程验证、说明书草稿和打包预检；人必须提供知情同意、真实动作、独立标签及争议内容复核。没有人时继续做能做的，不编造证据。
4. **五环节是职责隔离，不是每个小任务开五个模型。** 每个板块都有任务、实现、文书、验证、总控验收；无必要代码的文书任务允许“代码不适用”，不得为凑环节制造脚本。
5. **模型名称不是能力证明。** 以下为首轮岗位安排，不编性能排名。按补丁质量、可复现证据、越界情况再调换；五个模型不必同时忙。
6. **验证不等于验收。** GPT 提供独立测试与缺陷报告；最终总控根据代码、证据和缺失项决定通过、退回或阻塞。总控也不能替代真人、领域复核人或同意书签署人。
7. **删除“冷漠、禁止不确定措辞”的要求。** 输出应明确、可核查，但允许说未知。必须解释关键问题与证据；只报路径和通过/失败会隐藏风险。
8. **不把 Blender 固定成无限重建任务。** 先诊断当前文件，限两轮候选修复；仍有断指、穿模或教学歧义，就保留草稿状态，评估经许可、经核定的真人示范或清晰插图。不能为讨好检测器改错姿态，也不能由当前 831 顶点网格的失败推断所有整皮绑定都不可行。
9. **前端视觉重设计现在不启动。** 必需的镜像、对齐、无障碍状态及生命周期修复不属于视觉重设计，可以做。后续设计由 Gemini 承接，先释放 HTML/CSS 写锁。
10. **补充文档 §07A 是候选路线，不是强制算法流水线。** 不必须依次做 One Euro、SVM、随机森林、TCN。先定位问题；无独立收益就保留规则。训练、产品接入大模型、外部发布另行授权。
11. **交件口径纠正。** 仓库记载 09-21 邮件要求说明书＋Demo，截止 2026-10-23 24:00；过程与工具使用写入说明书，过程篇留作证据底稿。“必须三条 AI 否决”“固定八字母”“录屏必须先做 U”不是目前找到的赛方原文硬要求。只记录真实分歧，不凑数。
12. **不禁止一切指标，只禁止无依据指标。** 有独立标签和分母后可以报告探索性误接收等；必须注明样本量、条件、分组和不确定性，不能宣称总体真人准确率或学习效果已证实。

## 2. 本次核查的基线

- 分支 main，HEAD `9f5dfc732ad6fe7b37ace3b7ae8a198da022ba05`；大量已修改及未跟踪文件。实际基线是 HEAD＋当前工作区，不是单个提交。
- 已阅读现存项目日志、过程记录、交接、进展及 Git 压缩前日志 `8ce6aa0:手语陪练-项目日志与框架.md`。本次附件与仓库 `docs/手语学习陪练_反馈与中长期算法规划_2026-09-22.docx` 二进制完全相同，已提取全文；原理讲义也已读，属于旧实现说明，不得直接作为当前规格。
- 独立只读技术核查实跑 `bun practice/src/evaluate.test.js`：29 passed，退出码 0。这是现有合成/结构回归测试，不是完整正确性或真人验收。
- `accepted_practice=0`，U 仍 `pending_review`；没有真人 JSON fixtures。
- `practice/app.js:230–238` 无新帧直接返回；`passState.js:47–52` 重复帧分支先于过期检查。断流期间旧反馈仍可能保留。
- `practice/app.js:372–377` 导出检查未传帧龄/过期状态，断流期间可能导出旧快照。
- `practice/src/evaluate.test.js:622–645` 明确要求 fixtures 中 JSON 数量为零。它不是“真人回归入口已完成”，加入真样本反而会失败。
- `practice/src/bili-loop/eval-frames.mjs:49–59` 离线 evaluate 未传尺寸，与实时等尺度路径不同；旧 render JSON 用当前 CLI 回放得到 missing_size，旧结果表不是当前可复现成绩。
- Blender 09-23 进度比主日志更新：第4小时混合手失败，页面 PNG 回退第3小时。第5小时脚本存在不证明执行或通过。
- 本次没有做浏览器实拍、真人核定、断网演练或新渲染。不能把文件阅读或单测替代这些证据。

以上行号是本次快照定位，执行前重新定位。

## 3. 人员和写锁

| 角色 | 主责 | 写入范围 |
|---|---|---|
| 总控（本会话） | 目标、优先级、任务合同、接口裁定、最终验收 | 本文件、总体规划、代理合同、总日志当前状态；不写业务代码 |
| grok4.7 | 核心实现与集成 | practice 核心 JS、types、letters、核心测试、index/styles、sources、README 运行说明；排除下列独占目录 |
| ds4.1flash | 离线回放、评测与交付预检工具 | 第一轮仅 tools/assessment/；后续获得移交后写 practice/src/eval-handframe.mjs、bili-loop/eval-frames.mjs、render-loop/run.py；不得并改核心文件 |
| k3 | 内容出处、文书、伦理、证据索引 | docs Markdown（排除 agent-contract.md）、项目情况简述.md；不写 JS、letters.json 或示范 PNG |
| gemini3.8flash | 示范资产；后续前端设计 | 当前仅 blender/ 和 practice/content/demos/；候选先输出新目录。前端写锁本轮未授予 |
| gpt6astra | 独立验证、反例、风险审查 | 产品与文档只读；允许测试在隔离临时目录产生证据，不改基线、不自动更新 golden |
| 项目负责人及复核参与者 | 同意、真实试用、领域核对、最终对外提交 | 不能被模型姓名或模拟动作替代 |

写锁是文件所有权，不是 Git 自动保护。不同会话共用当前目录时不得切换分支、stash、reset、clean；不得互相运行覆盖性脚本。新 worktree 只带 HEAD、不带未提交和未跟踪文件：如果要隔离，须先由总控指定完整、可核验的工作区快照流程，否则沿用互斥写锁。当前不授权提交、推送、合并、发邮件或上传数据。

边界文件：types.js、letters.json、核心 judge 接口仅 Grok 写；DS 提变更请求，不能另造第二套规则。根日志由总控追加，K3提交事实摘要。验证期间冻结被测文件，修复后重新验受影响项，不能让验证器偷偷修复。

## 4. 板块任务卡：每块固定五环节

### P0 内容标准与能力边界（立即）

1. **总控布置：** 从8候选中形成2–3个可核定静态目标；同时保留U冲突清单。不预先规定正确手型，不把字体/ASL/二手视频当规范。
2. **代码实现：** Grok 保持 pending/demo 阻断；有核定依据后才同步 letters、规则及测试。区分内容已核定、规则覆盖完整、真人评估通过三种证据；不能仅换枚举就升 accepted。
3. **文书：** K3 逐项写规范版本、来源链接/本地文件、页码图号、掌向、指向、动作、左右手约定、覆盖限制、复核人和日期；AI预审与人工复核分栏。没有原文就写“证据未取得”。字体回退不一致时不能继续当教学依据。
4. **验证：** GPT 核对出处能否追溯、文案/PNG/规则是否对应、未核定项是否真的不能正式通过。领域人员复核内容，模型不得冒签。
5. **总控验收：** 每个正式开放目标必须证据链齐全；允许逐条验收，不等八条一起完成。内容通过不自动意味着规则/真人通过。

### P1 实时判定、摄像头与快照可靠性（立即，Grok唯一核心写入）

1. **总控布置：** 优先关闭断流旧成功和旧快照，其次检查坐标、质量门、状态门、停止/重开；不重写 UI，不重复新建导出系统。
2. **代码实现：** 按墙钟/单调时钟检查新鲜度，不能只等下一有效视频帧才失效；重复帧不计数但仍检查过期。下载动作独立验证快照帧龄。覆盖 readyState 不足、videoTime不变、track ended/mute、页面隐藏/恢复、异常、切目标、停止/重开；页面挂起恢复后在显示或导出之前失效，不假设后台定时器准时运行。保证输入质量门高于平滑/规则；尺度缺失或未知规则不可静默通过。代码/规则版本能定位实际工作区修订，不拿规范编号冒充规则版本。
3. **文书：** K3 记录 A01–A09 的“已有/缺陷/修复/验证未做”，说明帧计数与持续时长的差异。6帧和400ms仍是未验证参数，先不调参。Grok 提供接口与迁移事实。
4. **验证：** GPT 使用可控时间和行为回归验证“到通过后停止来帧超过窗口→旧通过取消、hold清零、导出拒绝→恢复重新累积”；另测切目标、无手、多手、坏点、无尺寸、镜像、权限拒绝、停镜重开。浏览器 mock 与真人分别报告；代码字符串检查不是行为测试。
5. **总控验收：** 新用例能捕获修前缺陷，修后通过；真实浏览器有对应证据，无设备项明确阻塞。不得报 A01–A09 全通过来掩盖未测浏览器项。

### P2 数据契约、离线回放与指标（立即做独立骨架，接口冻结后集成）

1. **总控布置：** DS 第一轮只读盘点旧格式与当前接口，在 tools/assessment/ 创建最小校验/回放工具；不与 Grok 抢核心文件。Grok固定接口后DS才能迁移旧CLI脚本。
2. **代码实现：** 复用核心判定，不复制规则。区分几何单帧、质量/内容单帧及连续序列结果；时间状态评估必须有序列及时间戳。区分原始采集与独立标签，标签记录 reviewedBy、labelVersion、expectedVerdict、expectedIssueCodes 及判定层级；targetLetterId/预测不是标签。新增样本读取与行为断言，移除“JSON必须永远为0”封锁，空目录明确报无真人数据而不是真人测试通过。旧文件缺尺寸则拒绝等价回放，不猜尺寸；历史产物只读，新报告写新目录。Grok负责修改核心测试中的旧空目录断言，DS不能越锁。
3. **文书：** K3 写采集/标注协议、数据字段、迁移限制、四类来源及同意边界，版本化关联采集—标签—规则—评测。
4. **验证：** GPT 测无标签、伪标签、坏元数据、已知正负例、unknown与blocked；工具冒烟用明确合成数据，不放进真人fixtures。统计误接收=独立错误样本中的通过比例，误拒绝=独立正确样本中的明确失败比例；无法判断/blocked单独列，报告各真值组覆盖率、原始分子分母和参与者/会话数，不排除unknown美化结果。
5. **总控验收：** 同一版本和输入可重复结果；无数据时无虚构百分比；历史结果不被悄悄覆盖；四类来源分开，单帧几何结果不能包装成最终通过。

### P3 教学示范资产（立即诊断，内容核定后定稿）

1. **总控布置：** Gemini 核对当前blend、备份、marker、脚本与页面PNG的对应关系；先解决掌指连接、残留手指、拇指片状、弯指拧皮。U不按“二指一定正确”继续定稿。
2. **代码/资产实现：** 保存原文件，另建候选blend和候选PNG；脚本支持明确输入输出，禁止默认覆盖主blend/线上PNG。每轮先少量渲染验证再批量。最多两轮有明确假设的修复，不开无期限“下一小时”。接口保持 `{GF0021.ID}_front.png`，只有总控批准候选后才替换正式资源。
3. **文书：** K3写资产来源、许可、版本和教学依据；Gemini提供构建/渲染命令、输入文件指纹、视角、已知缺陷与PROGRESS事实。HBM许可需追溯下载源，不能只抄CC0字样。
4. **验证：** GPT独立观察每张候选图的连贯手结构、指数量、拇指位置、遮挡、轮廓和相邻目标区别；领域复核人对照规范。Landmarker能否检出只记旁路实验，不是教学验收标准。
5. **总控验收：** 结构清楚且内容有依据才能叫正式示范；未核定标草稿。两轮仍不达标就记录限制并换素材路线，不用继续耗时替代进展。

### P4 真人探索与练习价值（P0/P1/P2达到采集门槛后）

1. **总控布置：** 先一次端到端采集冒烟，再做跨人探索。建议至少3位自愿成人、至少2种光照和2档距离，正确/典型错误/无法判断均包含；这是探索目标，不是统计充分性或普遍有效性证明。招不到人就报告实际覆盖，不填充人数。
2. **代码实现：** Grok修复采集时暴露的缺陷；DS用固定版本回放。暂不加闯关/排行榜。试用明确证明需要后，再给2–3目标增加顺序、错项复练、本机记录及删除入口；本机通过记录不叫“学会”。
3. **文书：** K3准备知情同意、匿名参与者/会话ID、设备条件、标签复核、原话和失败记录。默认先成人，未成年人有监护人同意和更谨慎的数据措施。导出许可不等于公开许可，原始数据不自动入公开库。
4. **验证：** GPT检查标签独立、训练调参与评估按人隔离、同会话/邻帧不跨集合；观察新人是否理解提示并调整，不只数绿灯。记录响应延迟、闪烁、误接收、误拒绝、覆盖率及样本条件。只有一人时只能验采集流程，不能称跨人评估。
5. **总控验收：** 工程流程通过、内容有效范围、探索结论分开签收。任何安全性/正式通过约束缺陷退回；没有真人证据不宣称学习收益。

### P5 说明书、离线交付与比赛证据（草稿现在开始，终稿随事实收敛）

1. **总控布置：** 说明书先写已有事实与待验证项，不等产品“完美”；执行以仓库记载的10-23截止倒排，赛方原件未见须标待核对。
2. **代码实现：** DS做只读打包预检和文件清单，输出新目录不覆盖现有包；Grok负责运行说明/来源页实际接线及最小离线缺陷修复。不擅自发送邮件或发布网站。
3. **文书：** K3写产品介绍、设计思路、设计过程、价值或创新、AI工具名与用途五部分；过程篇作底稿。分清未来安排与实际使用过的模型。清除旧讲义中handedness置信度、默认U正式通过、12测试等过时口径的引用，不悄悄改写历史。
4. **验证：** GPT在干净浏览器配置及断开外网但保留本地服务的条件下核查首次加载/刷新、模型/WASM/字体/PNG、权限失败及无摄像头回退；不得冒充已实施物理断网。核对包中无原始真人私密数据、班服文件、临时视频或无许可素材。演示录屏只能演示当前真实能力。
5. **总控验收：** 说明书与Demo、来源页、代码版本一致；截止/邮箱/队长名由负责人核对。缺真实姓名/赛方原件可完成草稿和预检，但阻塞最终对外发送，不编占位为正式值。

### P6 前端设计与制作（暂不启动）

1. **总控布置：** P0/P1完成、状态语义稳定后再签发；现在只保留需求：示范与自拍、可行动提示、明确未知/阻塞、键盘操作、对比度、手机适配，成功不能只靠颜色。
2. **代码实现：** Gemini取得HTML/CSS及纯展示组件写锁后负责设计制作；Grok只做接口适配，不并行改同一文件。保留原生技术路线，不因设计重写框架。
3. **文书：** K3写交互状态矩阵及文案边界，说明本机数据、权限、停止入口和未核定标识。
4. **验证：** GPT独立GUI验证窗口尺寸、镜像文字、骨架对齐、键盘、权限、停止重开与状态表达；模拟数据不能当真人演示。
5. **总控验收：** 视觉清晰且没有破坏状态门/隐私/性能；界面好看不补偿错误判定。本轮不启动P6视觉重设计，不授权Gemini生成页面、设计稿或修改CSS；P1必需的展示可靠性修复由Grok在明确任务范围内执行。

### P7 中长期算法（只保留路线，不启动训练）

1. **总控布置：** 先按失败类型决策：感知缺失修拍摄/感知；反馈抖动做未滤波对照；几何规则相似动作混淆才考虑小分类器；真实动态动作成为目标后再考虑序列模型。
2. **代码实现：** Grok负责旁路接口；DS负责固定数据划分和可复现实验。候选可为One Euro、SVM/随机森林、小TCN，但不要求全做。因果模型不看未来帧，模型冲突不得自动通过，保持规则解释与回退。
3. **文书：** K3登记实验假设、数据/标签/特征/模型版本、许可、离线体积、设备、延迟口径；未经总控验收不写已提升。
4. **验证：** GPT检查独立评估、按人划分、测试集不调参、误接收与覆盖率共同约束。看评估结果之前冻结误接收上限、最低覆盖率、延迟与回退条件；具体数值需基线和使用场景支持，当前未制定即不具备升级发布资格。
5. **总控验收：** 独立评估有可解释收益且满足事先门槛才考虑替换；无收益保留基线。AI自标自训、随机拆相邻帧、只报准确率、训练先斩后奏一律退回。

## 5. 执行顺序与时间预算

日期是工作窗口，不是自动解锁许可。遇阻塞缩正式范围，不降标准。

- 09-24～09-27：P0证据收集、P1残留修复、P2独立工具骨架、P3资产诊断；P5说明书结构并行。第一轮不做前端重设计、不训练。
- 09-28～10-04：接口冻结和P2集成，候选内容/资产对齐，独立浏览器验证，准备知情同意和真人采集。
- 10-05～10-11：P4真人探索，按失败修复；可验证内容不足则保留少量，不为八个凑数。
- 10-12～10-18：必要的练习价值改进、说明书定稿、离线演示与录屏。P6是否启动由总控根据稳定程度决定，不承诺必须重设计。
- 10-19～10-22：功能冻结，修阻断缺陷，提交包审计和负责人确认；10-23保留缓冲。对外发送由人确认执行。
- P7没有当前排期，不作为预选必做。

首轮可同时开 Grok、DS、K3、Gemini 四个互斥写入任务；GPT先建立只读验收矩阵，正式复验时冻结被测文件。先做每个prompt的第一轮，不把全部未来任务一次做完。

## 6. 发给每个执行模型的共同前缀（与一个专属Prompt一起复制）

```text
你参与 shouyu-peilian 项目，工作区 C:\Users\15424\Desktop\2026.9.15。
先读《项目总控与多模型执行Prompt-2026-09-24.md》、docs/agent-contract.md、README、docs/交接.md和你任务涉及的实际文件。以当前磁盘为事实，历史日志不是现行规格。本次基准曾为9f5dfc7且工作区很脏，开始时重新报告branch、HEAD、已有修改/未跟踪文件与你的写锁。
总控负责布置任务与最终验收；你不得把自测或验证报告称为已通过总控验收。发现总控任务有错误，要给证据和最小替代方案，不迎合也不为反对而反对。
保留用户现有修改；同一目录不切分支、不stash/reset/clean、不覆盖旧实验、不git add .。新worktree不包含未提交文件，不能拿旧HEAD当当前基线。本任务不授权commit/push/merge、外发邮件、上传真人数据、训练或依赖大改。
遵守文件写锁；涉及他人文件只提带证据的接口请求。做小步可逆改动，先补缺陷回归再修复。不等真人即可完成的工作直接做；需要真人、领域复核或许可时明确阻塞该子项，不造数据、不把整轮停在规划。
不得凭记忆/ASL/字体裁定国标，不降低标准赶绿灯。合成点、渲染、第三方视频、现场真人分开；目标/预测不是真值，导出同意不等于公开同意。U未核定，accepted_practice=0是本次基线，未经证据和总控不得升格。
每个板块必须交接五环节：总控任务依据、实现（不适用须说明）、文书事实、独立验证待办、提交总控验收。结尾给：基准与修改清单、根因与处理、实际命令/环境/退出码/测试数、证据路径、未执行项、风险、回滚方式和交付状态。未运行就写未运行，不能只写“应该可以”。
只执行下面专属Prompt的本轮任务，完成后停止扩范围。
```

### Prompt 1 — grok4.7：核心实现

```text
你是核心实现与集成席，负责P1，并承接P0/P2最终核心接线。不是总规划师，不重设计前端。
本轮只写 practice 核心JS、types、letters、核心测试、README必要运行说明；不写 demos、blender、DS独占离线脚本或docs文书。
1.【任务】先复现并修复断流旧成功与旧快照。重点看app.js loop无新帧早退、passState重复帧早退、downloadHandFrame不检查帧龄。核查其余A01–A09，不假定全没做，也不假定全已完成。
2.【实现】补无新帧期间新鲜度检查、下载入口独立时效门、恢复后重新累积；覆盖readyState不足、相同videoTime、异常、track结束、切目标、隐藏恢复、停镜重开。不要依赖后台定时器必然准时。保留现有等尺度/质量/内容门，不改U定义和判定阈值。检查快照元数据及版本可追溯性，不重做第二个导出器。
先加能在修前失败的行为测试，再改代码。现有29条仅为基线；源码字符串匹配不能代替行为断言。
3.【文书】给K3一份实际改动事实、接口合同、参数UNVERIFIED清单。为DS固定输入坐标、尺寸、判定层级、时序和输出格式，禁止离线复制规则；发现接口需扩大先报告具体必要性。
4.【验证】跑现有和新回归，提供修前/修后证据与浏览器可复现步骤；无法实拍就标未做，不用mock冒充真人。交给GPT独立复验。
5.【验收】向总控提交补丁范围、证据和残留。完成本轮可靠性修复后停止，不顺手加课程、训练、换框架或升accepted。
```

### Prompt 2 — ds4.1flash：离线评测与工程工具

```text
你是P2离线评测工具席，未来承接P5交付预检，不写核心算法。
本轮唯一写锁 tools/assessment/。其他文件只读；特别不要改app.js、evaluate.js、types.js、evaluate.test.js、letters.json或现有离线脚本。
1.【任务】盘点实时judge、schema v2、旧CLI、render-loop与bili-loop的契约差异，给出精确输入/输出表。重点确认旧离线调用缺尺寸、旧结果不可等价回放、几何通过不等于正式通过。
2.【实现】在独占目录写最小元数据校验/回放骨架，复用当前核心接口，不复制规则；接口未冻结时只实现不依赖变化部分并隔离适配层。原始采集与人工标签分离，声明expectedVerdict对应层级；无标签不计准确性，无数据明确报告0且真人验证未执行。冒烟用明确合成数据，不塞进真人fixtures。
所有旧文件只读，新报告写新目录。记录代码/规则/数据/标签版本。连续状态必须序列回放；单帧不能证明连续通过。输出各来源分组的误接收、误拒绝、unknown/blocked、覆盖率和原始分子分母。
3.【文书】给K3数据字典、迁移矩阵、运行命令和缺失材料；给Grok提出取消fixtures永远为空断言的核心修改要求，你不越锁修改。
4.【验证】自测缺尺寸、坏数值、伪标签、无标签、0样本、已知合成正负例；提供给GPT复跑。实际真人数据不存在就直说。
5.【验收】交总控。只有收到接口冻结与写锁移交通知后，第二轮才迁移旧eval-handframe/bili/render脚本。现在不训练、不运行会覆盖历史产物的脚本、不写假评测成绩。
```

### Prompt 3 — k3：内容证据与文书

```text
你是P0内容证据和所有板块的文书席，负责P5说明书草稿。你不是最终规范裁定人。
写锁 docs下Markdown（不含agent-contract.md）与项目情况简述.md；不改根规划/总日志、JS、letters.json、PNG或原DOCX。
1.【任务】阅读项目日志、AI-CODING过程、内容核定表、来源伦理、Blender进度及09-22反馈。把旧事实、当前事实、未来计划分开，按总控新裁定修正当前说明，不删除历史过程。
2.【实现】本轮不写业务代码。向Grok提交内容映射/状态门变更请求，向Gemini提供可追溯姿态规格；不能自己升accepted。检索GF0021—2019官方/可信原文及有效版本，区分发布页与实际图文证据；只找到404就记录检索路径和缺口，不造链接。
3.【文书】逐项整理8候选的规范版本、页码图号、手型/掌向/指向/动作、来源、冲突、复核状态。优先筛2–3个证据充分候选；U二指/四指只列证据，待领域复核。写同意/匿名标注模板、试用记录和证据索引。说明书按介绍、思路、过程、价值或创新、AI工具用途五块先成Markdown草稿，未知保留待验证；不虚构用户原话、模型使用经历或三条否决。
修正过程篇“不得并入说明书”、固定U正式通过等旧当前口径；历史段落标明当时状态。核对HBM/MediaPipe/字体等具体来源和许可，不能照抄结论。不要把未经核定的PNG叫标准手。
4.【验证】逐条建立主张—代码/命令/来源证据对应表。交GPT核查可追溯性、一致性和隐私；证据不足不得用漂亮措辞补齐。
5.【验收】提交总控：实际改文档、证据候选、仍需人工复核清单、未核验赛方原件与身份信息。没有真人时仍完成模板与草稿，不停在空提纲。本轮不导出排版DOCX、不写口播、不发邮件。
```

### Prompt 4 — gemini3.8flash：示范资产，前端暂缓

```text
你是P3示范资产席；未来兼任P6前端设计，但本轮没有前端设计授权，不改index/styles/app。
本轮写锁仅blender与practice/content/demos，任何输出先进入新的候选目录，不覆盖现用blend/PNG。
1.【任务】读blender/PROGRESS.md、现有脚本及内容核定表，确认第3/4小时blend与PNG对应关系。第5小时脚本存在不代表完成；不要直接运行会覆盖主blend的脚本。
2.【实现】先记录当前文件/marker/rig/渲染接口，保存原资产；诊断掌指断开、鬼影手指、拇指片状、U/A拧皮。基于明确假设最多做两轮小范围候选修复，每轮先少量预览再批量。保持GF0021字母ID与文件命名；脚本明确输入输出。U规范未定，不继续按二指结论作正式教学定稿。当前可做模型结构修复，具体姿态等K3可追溯规格和人工复核。
3.【文书】记录候选来源、许可线索、命令、输入/输出文件、渲染版本、视角和真实失败；把事实交K3同步来源文书。PROGRESS不把“脚本写好”记成图已通过。
4.【验证】自检完整手结构、指数量、掌指连接、拇指、遮挡、轮廓和教学可读性，再交GPT独立视觉检查与领域人员规范复核。Landmarker检出率不是教学正确性指标。
5.【验收】把候选图和可复现命令提交总控；未经批准不替换页面资源。两轮仍不达标就交清楚的失败报告及经许可真人示范/插图替代需求，不无限重建。不要启动前端稿或训练。
```

### Prompt 5 — gpt6astra：独立验证

```text
你是独立验证席，不是修复席，不是最终验收人。只读产品/文档/资产；仅可在隔离临时目录保存测试证据，不改源码、标签、golden、阈值或历史结果。
1.【任务】先读总控计划及各执行席的实际交付，记录被测文件指纹/版本。验证期间基线必须固定；发现并发变化则本次结果标失效。对每项分别写已验证、未验证、阻塞，不照抄作者结论。
2.【实现审查】查看差异和行为是否解决根因。重点查断流旧成功/旧导出、坐标尺度、输入质量、内容状态门、重复帧、停镜重开、版本追溯及离线/实时路径一致性。验证器不能顺手修复，问题退回原作者。
3.【文书核验】核对主张与证据、规范出处、示范一致性、真人标签独立性、许可、四类数据边界、比赛说明书五块。确认未来模型安排没有被写成历史使用记录。
4.【独立验证】自行复跑实际命令；对稳定基线做单元/序列回放/浏览器流程/资产视觉/离线包检查。先复现修前问题再证修后，做不到则注明证据限制。浏览器mock不是实拍，合成点不是真人，静态截图不证明摄像头已释放，CSS字符串不证明视频骨架对齐。没有设备/真人/可断网环境就标阻塞，不填通过。不得用假摄像头成功证明真人效果。
报告缺陷严重度：P0错误正式通过/隐私泄漏，P1旧状态/不可复现/主要流程阻断，P2非阻断体验/文档问题；每条给文件行号、复现步骤、实际/预期、证据路径。
5.【提交总控】输出验证建议：可验收、退回修复或证据阻塞，明确范围。只有总控可以做最终验收结论；你的“测试通过”不代表产品完成。
```

## 7. 交回本会话的总控验收 Prompt

```text
请按《项目总控与多模型执行Prompt-2026-09-24.md》验收本轮交付。
先确认当前工作区、实现补丁、作者自测、K3文书和GPT独立验证针对同一版本，再检查实际文件与关键证据。不要只根据摘要签收，也不要重新做没有价值的全量审阅。
每个板块检查五环节是否齐全：你布置的范围、实际实现、文书事实、独立验证、你的验收。
分别给出：通过（范围/版本/证据）、退回（根因/负责模型/最小修复）、阻塞（缺失真人/规范/许可/设备证据）。内容核定、代码正确、真人效果、视觉设计、可交件必须分开判定。
未核定内容正式通过、旧帧冒充当前、伪标签/虚构数据、未授权上传或覆盖他人修改，一律不通过。不要为了按日期交件放宽标准；缩正式范围而不是伪造完成。
若证据支持修复，给原负责人最小返工任务；不自动开前端、训练或扩功能。不替用户发邮件、推送或公开数据。
```

## 8. 本轮规划交付状态

本文件以及旧规划入口的状态纠正是本轮实际交付。五模型prompt尚未等于五模型完成实现；业务代码/示范资产/真人数据没有因本次规划而改变。原补充DOCX保留为审阅来源，新裁定在本文件逐条说明。当前不提交、不推送、不发布。
