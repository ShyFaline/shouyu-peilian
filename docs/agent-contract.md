# Agent 合同

更新入口（2026-09-26）：先读仓库根 [AGENTS.md](../AGENTS.md)。真人示范暂缓，先联调现有项目；每个独立可验证改动完成后按根规则 commit 并 push 到任务分支；不自动合并或部署 main。独立验证现由 K3 验证窗口承担，与 K3 文书窗口分离。下文旧“禁止提交推送”和 GPT 验证席条目已被替换；其余写锁继续有效。其他窗口已有改动保留，不代提交。

历史更新：2026-09-24。执行任务、五环节与验收条件见 [总控与多模型执行 Prompt](../项目总控与多模型执行Prompt-2026-09-24.md)。本合同替换旧三席编制，不改写历史使用记录。

## 原则

- 输出明确、可核查，说明根因、证据及限制；允许表达未知，不用自信语气替代证据。
- 每个板块包含：总控布置、实现、文书、独立验证、总控验收。无必要代码的任务明确不适用，不制造工作。
- 写锁互斥。涉及他人文件先提接口请求；不得同时修改同一文件。
- 当前目录有大量未提交和未跟踪文件，禁止重置、清理、stash或并发切分支。新worktree默认不含这些改动，不得据此声称复现当前基线。
- 本轮不授权提交、推送、合并、发邮件、上传数据或训练。原始实验、源附件与他人修改不得覆盖。

## 文件所有权

| 席位 | 写锁 |
|---|---|
| 总控（本会话） | 总控规划、总体目标、项目总日志当前状态、本文；不写业务代码 |
| grok4.7 核心实现 | practice核心JS、types、letters、核心测试、index/styles、sources、README运行说明；排除其他席位独占范围 |
| ds4.1flash 评测工具 | 第一轮仅tools/assessment/；接口冻结和明确移交后才写eval-handframe.mjs、bili-loop/eval-frames.mjs、render-loop/run.py |
| k3 内容与文书 | docs下Markdown（排除本文）、项目情况简述.md；不改规则或资产，不代签人工核定 |
| gemini3.8flash 示范资产 | blender/、practice/content/demos/；候选先另存，替换现用资产须总控验收；前端设计暂不启动 |
| gpt6astra 独立验证 | 产品、文档与资产只读；可在隔离临时目录保存执行证据，不修源码、不改标签或golden |

`types.js`、`letters.json`、核心判定接口仅Grok写。DS复用核心接口，不复制规则。K3整理事实，根项目日志由总控追加。未来前端设计启动时，HTML/CSS与纯展示文件须从Grok明确移交给Gemini，不共享写锁。

## 完成与验收

- 自测、独立验证、总控验收是三件事。执行席不得自封最终通过。
- 验证时冻结被测文件；修复后重新验证受影响项，记录准确版本。
- 内容依据、工程可靠性、真人评估、教学示范、交件就绪分别签收；不能相互替代。
- 真人同意、真实试用、独立标签、争议内容复核和对外发送确认由相应人员提供；AI不能冒名完成。缺这些材料不阻塞其他可独立完成的工程工作。
- 合成点、渲染图、第三方抽帧、现场真人分开；无证据不报准确率/学习收益。U争议未解决前不升格，未核定内容不得正式通过。
