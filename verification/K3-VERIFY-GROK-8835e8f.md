# K3 独立验证：Grok 交付验收表

日期：2026-09-26。验证席：K3 验证窗口（与 K3 文书窗口分离）。只检验，未修任何代码/文书/素材。

## 0. 被测版本与环境

- **固定候选**：`8835e8f`（分支 `core/demo-image-tests-20260926` 在验证开始时指向它）。验证期间该分支被推进到 `82818f5`（合入 DS 评测工具 + Gemini 文案）；本报告只对冻结的 `8835e8f` 有效。
- **冻结方法**：`git worktree add 工作副本/K3验证 -b verify/k3-grok-8835e8f-20260926 8835e8f`，在副本内测试。副本与主目录全部 `practice/src/` blob 逐一致（唯一差异是候选新增的 reliability.test.js 本身）；app.js / index.html / letters.json 与主目录相同。规则与文书变化不影响本报告对产品流程的结论。
- **环境**：Windows 11，Bun 1.4.1，Node 22.22.2（`.workbuddy` 内 22.22.2-3），ZCode 内置浏览器（虚拟摄像头，无真实手部画面）。
- 候选改动范围：`AGENTS.md`（根规则）、`docs/agent-contract.md`（4 行更新入口）、`practice/src/reliability.test.js`（+3 个示范图竞态/失败用例）。**无夹带**：无新素材、无 2D/3D 手模、无重设计、无训练、无规则/阈值放宽。

## 1. 验收表

| 功能 | 结果 | 对用户的影响 | 证据 | 负责处理者 |
|---|---|---|---|---|
| 打开页面、默认 U 不开放练习 | 通过 | 未核定内容不会误导初学者 | 初始「暂不判定」 | — |
| 浏览示范与图鉴（32 字母、易混标记） | 通过 | 可浏览全部字母 | 32 按钮、E/M/N/S/Ê 标「易混」 | — |
| 切换目标（U→V→A→图鉴 Q） | 通过 | 切换即时、无旧状态残留 | 状态/图示/说明同步切换 | — |
| 开摄像头 | 通过（虚拟设备） | 可进入练习画面 | 「识别在本机进行，视频不上传」 | — |
| 镜像与反馈文字 | 通过 | 镜像只翻手不翻字，不误导 | `.stage-media` matrix(-1)，文字 transform:none | — |
| 无手判定状态 | 通过 | 不编造结果 | 「暂时无法判断」 | — |
| 无手导出限制 | 通过 | 不会导出假快照 | 「当前不能导出（无手…）」 | — |
| 停止→再次打开 | 通过 | 摄像头能干净释放并重开 | 旧轨道 ended、srcObject 清空、新流新对象 | — |
| 权限失败 | 通过 | 拒绝权限后提示清楚、可重试 | 「摄像头或模型失败，可先看左边示范」、按钮恢复 | — |
| 断流后旧结果 | 通过 | 轨道 ended 事件→页面停机、按钮恢复；`track.stop()` 本身不触发事件属浏览器规范行为，非缺陷 | 手动派发 ended 实测 | — |
| 过期/多手/切目标后导出限制 | 通过（VM 层） | 快照有时效约束 | reliability.test.js 29/29 含对应用例 | — |
| 本地离线回放 | 通过 | 旧缺尺寸记录被拒为「不可评估」而非误判 | eval-handframe exit 2 + unevaluable | — |
| 资源与来源页 | 通过 | 全部资源本机加载、无外网请求；来源页 4 条齐全可开 | 0 外部资源 | — |
| 外网隔离（保留本地服务） | 通过 | 离线可用主张成立 | 候选副本 8766 端口全部资源 127.0.0.1 同源 | — |
| 核心回归 | 通过 | — | evaluate 31/31（Bun）；reliability 29/29（Node） | — |
| 真实摄像头几何通过 | 未执行 | 无法确认真人比对手型会被判对 | 虚拟摄像头无手部画面 | 需真机验证 |
| 真实手机/桌面 Chrome | 未执行 | 移动端布局未经实机 | 仅 IAB 浏览器 | 需真机验证 |
| 真实挂起/恢复（息屏、切后台） | 未执行 | 仅合成时钟覆盖 | — | 需真机验证 |
| 页面「标准手」措辞 | 失败（非候选引入） | 用户可能误以为示范已按国标核定 | `app.js:131` alt 仍写「标准手示范」；核定表说不是标准示范 | Grok（页面文案）/ k3（来源页） |

## 2. 文书核对（仅与本次交付相关）

- 候选内 `agent-contract.md` 更新如实：指向 AGENTS.md、写明验证席与文书席分离、保留历史段。通过。
- 候选未宣称「已上线/已推送」类不实表述；AGENTS.md 内容与现行规则一致。
- 遗留（不影响候选本身，但影响整站收口）：`sources.html`「未找到官方 URL」与 PDF 检索记录矛盾；证据卡 B/Y/L 引用的候选路径与哈希不存在；说明书/简述的测试数字过时。详见第四轮报告 R4-02/R4-03/R4-05。

## 3. 结论

- **可以验收**：`8835e8f` 候选（示范图竞态/失败测试 + 规则文档）在合成与虚拟设备范围内完整跑通现有产品流程。
- **必须修复后再验**：页面「标准手」措辞（R4-04）；文书三处（R4-01/02/03）。
- **环境不足**：真实摄像头、真机、挂起恢复、真人评估。
- 负责人此前真人验收：保留为负责人确认，本席未冒充实测。

## 4. 给老板的通俗版

产品在电脑上已经能完整走一遍：打开网页、看示范、换字母、开摄像头、照镜子式练习、导出受限保护、停了再开，都正常；识别计算全在本机，不上传视频。**剩下影响使用的**：真实摄像头前比对手型能否判对还没在真机上测过；手机浏览器没测过；示范图页面还写着「标准手」但其实还没人工按国标核定。这几项解决前，不建议当作正式可交付产品。

—— K3 验证窗口（独立验证建议，非最终验收）
