# 手语学习陪练 · 项目日志与框架

> 备赛过程记录与结构草稿，不是最终规格。  
> 方向可以长，细节可以改。任何模块都可以被更好的想法替换。
>
> 2026-09-22 外部审阅（对照仓库 `9f5dfc7`）已收入：  
> [`docs/手语学习陪练_反馈与中长期算法规划_2026-09-22.docx`](docs/手语学习陪练_反馈与中长期算法规划_2026-09-22.docx)  
> 该文件保留为审阅来源。**2026-09-24 当前执行以 [总控与多模型执行 Prompt](项目总控与多模型执行Prompt-2026-09-24.md) 为准**：核定内容与通用可靠性修复并行，正式开放必须经过证据关口；不靠继续加功能证明进展。

---

## 〇、现在站在哪

产品：本地国标手指字母**陪练**，不是翻译器。默认 U。主路径 `A B U V L Y I W`。

仓库：https://github.com/ShyFaline/shouyu-peilian  
路径：`C:\Users\15424\Desktop\2026.9.15`  
交接：`docs/交接.md`（交接稿锚点 `3457ffb`；仓库 HEAD `9f5dfc7`；工作区未提交）。规划：`总体目标与并行拆分.md`。缺口表：`docs/内容核定表.md`（未完成人工对照原文，不是验收）。

交件以 2026-09-21 邮件为准：说明书 + Demo，预选 2026-10-23 24:00。过程篇是说明书底稿，不替代说明书。

| 阶段 | 状态 |
|---|---|
| S0 看见手 | 完成 |
| S1 合成闭环 | 完成。夹具现为 `bun practice/src/evaluate.test.js` **29 passed**（工作区，合成点，不是真人；不是内容正确） |
| S1.5 自渲图过 Landmarker | 完成。8 张里 6 张无手；不是真人准确率 |
| B 站抽检 | 完成。实拍多数有手；无字母标签，不报准确率 |
| **内容核定（对照 GF 0021—2019 原文与图示）** | **未做。** 已建缺口表 `docs/内容核定表.md`；U 的二指 / 四指冲突仍 open，本轮不裁定 |
| A01–A09 可靠性修复 | 已有实现，09-24 独立只读核查实跑 29 passed；仍发现断流旧反馈/旧快照、真人夹具入口和离线坐标契约缺口，未整体验收。工作区未提交 |
| S2 真人闭环 | 未开始 |
| 说明书 / Demo 录屏 | 未成稿 / 未做 |
| S3 手图精修 | 已有 09-23 重建尝试，未验收；第4小时候选失败，页面PNG据进度记录回退第3小时。详见 blender/PROGRESS.md。示范以教学正确为先 |

技术上不准备和别人比「谁的识别模型更大」。感知可以站在已有关键点方案上（MediaPipe Hands），把精力放在：

- 练什么、按什么顺序练（先核定，再开放正式通过）
- 错了怎么被看懂；看不清时如何拒绝判定
- 怎样让一个完全不会的人愿意练完第一关
- 示范必须是手，不能只是字形

模型可以换，课程可以长，交互可以重做。下面只是一张可以走路的地图。

---

## 一、项目日志

（按时间往下追加。日期可改，事实以当时为准。）

### 2026-09 选题阶段

- 比赛要的是可运行的产品，以及用 AI Coding 把产品做出来的全过程；不鼓励只交一段提示词或一个对话 skill。
- 讨论过教育刷题、体态康复、合同/医嘱类文本工具等。文本类开源极多；视觉类如果做成聊天套壳，也容易被看成 skill。
- 逐渐收到「文明向度」：技术有没有帮到具体的人。阅读障碍辅助、手语陪练进入短名单。
- 手语方向的直觉：摄像头、手、对错、当场演示——评委能参与。风险是光照和相似手型；需要用几天实验说话，而不是用想象说话。

### 调研

已看到的开源大致分三类，都不自动等于我们的产品：

1. **识别 / 翻译**：手语 → 字。国内课设和仓库多走这条路。
2. **字母检测**：尤其是 ASL 手指拼写 + 关键点，技术底座很常见。
3. **教学向**：例如 SignBridge（ASL / PSL、浏览器、跟练、虚拟人）。形态接近，语种和纠错粒度不同。

对我们有用的结论：

- 关键点、分类器、摄像头流水线不必从零发明，但**要标明来源**。
- 差异不在「再训一个更大的网」，而在国标内容、错误被解释的方式、以及真实练习场景。
- 若某天出现更好的手部感知或更合适的国标数据，可以整层替换，不必推翻产品。

### 实验原则

只验证一件事：**机器能不能稳定分开两只很像的手。**

- 选一对易混手型（国标手指字母里外形接近的两个）。
- 不同人、不同光照、不同距离，各采集一段。
- 只看：关键点是否稳、两类是否分得开、失败时失败在哪（遮挡、侧光、镜面、左右手……）。

实验过：这条路可以走。  
实验挂：记下挂在哪，再决定是补采集、改判定、缩小第一期范围，还是把主产品转到阅读辅助——那也是一条完整的路，不是失败。

四类数据必须分开，不能混报成绩：人工合成点、Blender 渲染图、第三方实拍抽帧、自愿参与者现场采集。前两类测代码与管线；第三类只有许可与标签明确时才有评估用途。

### 开发

细节与交件清单见 `docs/交接.md`。下面只留日期事实。

**2026-09-15** 练习页能跑：摄像头、MediaPipe 离线、到位/不到位。

**2026-09-17** 规划锁定：陪练不是翻译器；主路径 8 字母；Blender 是内容工厂，不进识别循环。

**2026-09-18** 默认 U、图鉴、8 张草稿 PNG。

**2026-09-20** `evaluate.test.js` 12 passed（合成点）。JSON 导出默认关。渲染图过 Landmarker：多数无手。B 站抽检：实拍多数有手，无字母标签，不报准确率。真人未试。仓库 U 规则是食中并拢；有的国标讲解 U 是四指并拢。未改 rules。

**2026-09-21** 细则邮件：说明书 + Demo，10-23 24:00。过程写入说明书。交接见 `docs/交接.md`。

**2026-09-22** 收到外部审阅（核查版本 `9f5dfc7`），全文见 `docs/手语学习陪练_反馈与中长期算法规划_2026-09-22.docx`。

审阅认可：把识别转成可改动作的提示；用现成感知、不假装自研检测网；本机运行与导出已进流程；`evaluate` 已拆成纯函数并有合成测试；失败记录没有包装成准确率。

审阅要求下一阶段从「继续增加东西」转到「教的内容正确、判定有依据、失败能解释」：

1. **内容先于 UI。** 建立主路径内容核定表（规范版本、图号、掌向、指向、是否有动作、复核人）。文字、PNG、字体回退必须与同一条定义一致。U 的二指 / 四指冲突先列证据，核定后再同步文案、示范、规则与测试。
2. **能力状态是程序约束。** 建议状态：待核定 / 仅示范 / 局部姿态练习 / 已验收练习。未核定、仅图鉴、J/Z 只核静态手型，均不得显示为完整字母已掌握。M/N/S 规则相同，不能继续走统一「到位」。
3. **可靠性缺口 A01–A09**（坐标等尺度、handedness 不是姿态质量、退化输入应拒绝、拇指规则与文案不一致、评价文字被镜像、旧通过状态在断流后仍累积、导出可能是旧帧或错标签、真实夹具只断言类型、缺停止摄像头与可核查来源链接）。
4. **阶段顺序：** 内容核定 → 修复 A01–A09 → 小范围真人验证 → 练习价值 → 有证据后再谈算法升级。中长期算法见该文件 §07A：先区分不稳定与不正确，再旁路轻量学习，动态需求成立后才评估时序网络。本轮不训练、不换大模型。

**2026-09-22（同日，规划 → 批判修正 → 编码，未 commit）**

工作流：规划席 → 批判席否决「按原文开工」和「把枚举当成内容核定」→ 编码席按裁定后最小集合落地。未 commit。仓库 HEAD 仍是 `9f5dfc7`。

编码落地（工作区，不是验收）：A01–A09。judge 返回 `decision`: pass / fail / undetermined / blocked。`pending_review` / `demo_only` 不得正式到位。看不清 → 暂时无法判断。A01 等尺度；A02 不用 `handedness.score` 当质量；A03 退化拒绝；A04 U/V 补 thumb 收起（未改二指/四指、未降阈值）；A05 评价文字不再镜像；A06 连续有效；A07 导出 schema v2，禁止 `expectedVerdict` 当标签；A09 可停摄像头。`passFrames=6`、`maxGapMs=400` 标 UNVERIFIED。默认字母仍是 U，开页不能绿灯。

测试：`bun practice/src/evaluate.test.js` → **28 passed**，退出码 0。环境 bun 1.4.1。合成测试，不是真人。不是内容正确，不是国标已核定。

`letters.json` `practiceStatus`：`pose_practice` 9，`pending_review` 18，`demo_only` 5，`accepted_practice` **0**。U 仍 `pending_review`（how 仍是「食指、中指并拢伸直朝上，其余收起」；curled 现为 ring, pinky, thumb）。V A B L Y I W J Z = `pose_practice`（程序约束，不是已对照国标）。M N S E ê = `demo_only`。其余 `pending_review`。没有对照 GF 0021—2019 原文与图示的人工复核。U 二指/四指冲突仍 open。

`sources.html`：MediaPipe Web、SignPinyin 可点链接；教育部公文页 404，已写「未找到官方 URL」，未造假链接。

缺口表：`docs/内容核定表.md`（未完成人工对照原文，不是验收证明）。

**2026-09-22（同日，第1轮残留修复，未 commit）**

停镜：`stopLive` 清 `#verdict` 回 idle。切字母：`selectLetter` 清 `lastSnapshot`。fail 大字「对照左边改动作」；去掉「把手指放到位」。A01 补近阈值等尺度测试；A08 空断言删除。`bun practice/src/evaluate.test.js` → **28 passed**，退出码 0（合成点，不是真人）。U 仍 `pending_review`；`accepted_practice`=0；S2 未做。不是内容核定，不是验收。

**2026-09-22（同日，第2轮，未 commit）**

`bun practice/src/evaluate.test.js` → **29 passed**（合成点，不是真人）。交接 §1 删「12 passed」；§7 录屏跟页面。`app.js` 摄像头失败不再覆盖 idle。A08 空目录真断言。读 `app.js` 锁 `stopLive` / `selectLetter`。U 仍 `pending_review`。S2 未做。不是内容核定，不是验收。

**2026-09-22（同日，第3轮，未 commit）**

CLI 缺尺寸拒绝；render-loop 不再把 handedness 当 conf。U 仍 pending。S2 未做。

**2026-09-22（同日，第4轮，未 commit）**

只改过程篇/总体目标现状句。代码空转。U 仍 pending。

**2026-09-22（同日，第5轮，未 commit）**

停扩。代码零改。编码空转。HEAD 仍是 `9f5dfc7`。工作区 `bun practice/src/evaluate.test.js` **29 passed** 仍是合成点，不是核定，不是验收。待补收成 H1–H10（人工关口，全部未完成）。

### 2026-09-24 总控重整（规划交付，不是业务修复）

- 通读现存日志、过程记录、交接、09-23手模进度，以及Git压缩前日志 `8ce6aa0`；完整提取外部反馈与原理讲义。此次附件与仓库09-22反馈DOCX二进制相同。
- 独立只读技术核查实跑 `bun practice/src/evaluate.test.js`：29 passed，退出码0；发现无新帧时旧通过/快照未主动失效、fixtures测试要求JSON永远为0、旧离线evaluate缺尺寸等残留。不是浏览器实拍或真人验收。
- 新增 `项目总控与多模型执行Prompt-2026-09-24.md`：8个板块全部包含总控任务、实现、文书、独立验证、总控验收。五模型是待执行岗位，不是已使用证明：Grok核心、DS评测工具、K3内容文书、Gemini资产及未来前端、GPT独立验证。总控最终验收。
- 修订：8字母为候选池，先核定2–3个静态目标；U不预判答案。核定与通用修复并行，不再把所有关口都归人工；前端重设计暂缓，不训练。说明书包含过程与工具用途。
- 原DOCX和历史记录保留；当前仅更新规划、合同与入口，没有改业务代码、示范资产或采集真人数据；未commit、未push。

### 关口 H1–H10（09-22历史任务表，未完成；现行责任拆分见09-24总控规划）

完成时禁止把本表写成已完成。合成 29 passed 不是其中任何一条。

| 关口 | 内容 | 谁 | 状态 |
|---|---|---|---|
| H1 | 对照 GF 0021—2019 原文与图示核定主路径 8 字母（页码/图号、掌向、指向、动静） | 人 | 未完成 |
| H2 | U 二指/四指列原文证据并裁定。未裁定不准改 rules、不准升格 | 人 | 未完成 |
| H3 | 核定表补复核人与日期。文案/PNG/字体/rules 对同一条。pose_practice ≠ 已对照国标 | 人 | 未完成 |
| H4 | 浏览器实拍核 A01–A09。合成 29 条不是这条 | 人 | 未完成 |
| H5 | passFrames=6 / maxGapMs=400 真人标定。现 UNVERIFIED | 人 | 未完成 |
| H6 | S2：已核定范围内至少一人试。JSON→fixtures，原话→试用户记录。U 仍 pending，不准用 U 绿灯充 S2 | 人 | 未完成 |
| H7 | 说明书成稿（邮件五块）。过程篇只是底稿 | 人 | 未完成 |
| H8 | Demo 录屏。路径：开摄像头→比 U（暂不判定）→对照左边改动作→切 V。不写口播 | 人 | 未完成 |
| H9 | 队长姓名写入简述和 zip 文件名 | 人 | 未完成 |
| H10 | 提交包自检 + 断网演练。特教/听障反馈选做，没有不编。S3 仍不做 | 人 | 未完成 |

---

## 二、框架（开放结构，不是施工图）

整体可以想成四层。层与层之间用很窄的接口说话，所以任何一层都可以被换掉。

```
        人（学习者 / 示范者 / 助教）
                    │
                    ▼
        ┌───────────────────┐
        │  体验层            │  课、练、测、玩、回顾
        └─────────┬─────────┘
                  │ 手型是否到位、差在哪、下一步练什么
        ┌─────────▼─────────┐
        │  理解层            │  对齐、打分、解释、建议
        └─────────┬─────────┘
                  │ 关键点 / 轨迹 / 置信度
        ┌─────────▼─────────┐
        │  感知层            │  摄像头 → 手的几何
        └─────────┬─────────┘
                  │
        ┌─────────▼─────────┐
        │  内容层            │  标准、词表、错误样例、故事
        └───────────────────┘
```

### 1. 感知层：看见手

**职责**：把一帧或一段视频变成「手在怎样」。

**当前较省事的路**：现成手部关键点，本地跑，不把用户视频默认上传。

**保持开放的**：

- 单手 / 双手、静态手型 / 带轨迹的词
- 前置摄像头镜像、左右手、半身入画
- 将来若关键点不够，可以加深度、多视角、或换别的姿态模型
- 不把「自训一个超过别人的检测网」当成目标。感知是基础设施，好用、稳、可替换即可

输出尽量是中性的：点、线、输入质量、左右手分类、是否出框、帧是否新鲜。不要在这一层写死「这是字母 M」。左右手分类得分不是整只手的姿态质量；接口没有提供的可靠度字段不要自行补 1。

看不清、缺关键点、几何退化时，这一层应让理解层拒绝判定，而不是继续往下当成「做错了」。

### 2. 理解层：看见之后怎么说人话

**职责**：当前手型离目标有多近；近在哪、远在哪；要不要再给一次机会；材料不够时承认无法判断。

这里才是产品可能拉开距离的地方，也是最不该写死的地方。可以探索的，包括但不限于：

- 整只手一个分数
- 逐指、逐关节的偏差（「哪根手指还没伸直」）
- 只判断「像 / 不像」，把解释交给示范动画
- 对时间敏感的词：轨迹、节奏，而不只是最后一帧
- 不同严格度：入门宽松、闯关收紧
- 用规则、用小分类器、用检索最近邻、或几者并存——以实验为准

原则只有一条：**解释要让人能改动作**，而不是只丢一个 87 分。未知、未核定、过期或低质量输入不得给正式通过。空规则或不可区分的规则，不得证明多个字母都已识别。

几何计算须在等尺度空间进行；画面宽高比变化不应改变判定。文案要求约束的手指（含拇指贴掌、接触、位置）必须写进规则，不能只写在提示里。

### 3. 体验层：人愿意练下去

**职责**：为什么打开、第一分钟干什么、练砸了怎么办、练会了怎么被看见。

下面都是可选方向，不是功能清单：

- 跟读：看示范 → 自己比 → 即时反馈
- 对照：分屏标准 / 自己
- 闯关、测验、错题、限时
- 拼词、打招呼小任务（字母会了以后才有意义）
- 双人：一方示范一方打分，或家长和孩子一起
- 无摄像头模式：先看、先模仿，再打开识别（照顾没摄像头或光线极差的场合）

界面可以是网页、可以是以后的小程序或 App。第一期能在浏览器里跑起来，已经够比赛演示。

当前必须守住的体验事实：评价文字不被镜像反写；视频与骨架几何一致；检测中断后旧的「连续通过」失效；停止摄像头后能释放轨道并重新打开。进度和复练记录先留在本机。

### 4. 内容层：练的到底是什么

**职责**：标准从哪来、第一期教什么、错误样例怎么积累。

倾向（可推翻）：

- 公开、可引用的国标 / 词典，不发明一种「我们队的手语」
- 第一期宁可窄：静态或准静态、数量可控、每条都有正反例
- 自录示范可以，但要记下拍摄条件，方便别人复现，也方便以后替换成更专业的示范
- 方言、地区变体：可以记录「存在差异」，第一期不必全覆盖；被问到就诚实说范围

内容是可以生长的资产：字母、词、短句、场景对话，都不必第一天出现。

当前缺口：规则、示范、规范尚未逐项对照 GF 0021—2019 原文与图示。在核定完成前，主路径条目的「正确答案」不能只靠现有合成测试全绿来证明。示范图先检查教学清晰度与标准一致性；渲染图多数未被 Landmarker 看见，不能直接推出真人也不可用，更不能把示范改成检测器喜欢但规范不对的姿态。

---

## 三、一条最小能跑的线（仅用于「先看见东西」）

用来避免空转，**不是最终范围**：

1. 打开摄像头，画出一只手的骨架
2. 库里至少两个目标手型
3. 比一个，给反馈（对 / 不对，或再细一点）
4. 能换人、换光，再试一次

这条线已经通了。下一刀不是再加十个功能，而是：核定这两只手教的是不是同一套国标，以及失败时失败在输入、规则还是内容。

---

## 四、比赛叙事（可改，勿写满）

可以同时讲三件事，比重自己调：

1. **产品**：一个能练的工具，不是又一个翻译 Demo
2. **过程**：需求、实验、编码、试用人，怎样和 AI Coding 工具一起完成；哪些决策是人做的
3. **向度**：具体的人（家长、志愿者、窗口、同学）少了一层「没人纠错」的障碍；不夸大、不承诺治好沟通问题

引用他人代码、模型、词典，单独开一页来源。旧项目不拿来充新。

展示时分开报告：能否看到手、有效姿态是否被拒绝、明确错误是否被放行、多少次无法判断、提示能否帮助改正。每项写清分子分母、样本数、条件与版本。不要用一个孤立准确率，也不要把「已经搭了流程」写成「学习效果已经验证」。

---

## 五、风险与可以故意留下的空白

| 可能翻车的地方 | 不一定要立刻填上的空白 |
|---|---|
| 规范、文案、示范、规则互相冲突（已暴露：U） | 要不要 3D 虚拟人 |
| 光照、遮挡、镜面、左右手；画面比例改变判定 | 要不要做连续语句翻译 |
| 外形接近的手型；相同规则冒充可区分 | 要不要做商业培训包 |
| 没有特教老师时内容会飘 | 要不要 App 上架 |
| 现场演示网络 / 权限 / 浏览器兼容 | 账号与云端视频 |
| 把合成测试全绿当成内容正确 | 本轮训练深度网络 |

空白留给以后的灵感。现在只保证：有人能在镜头前练起来；已核定的内容能被可靠判断；不可靠的情况能诚实拒绝。

---

## 六、审阅后的阶段（2026-09-22 起）

完整表述、验收矩阵与中长期算法说明以审阅原文为准，不在日志里复述执行禁令。

| 阶段 | 只解决什么 | 完成时看得见什么 |
|---|---|---|
| 0 内容先核定 | 核对主路径规范；有争议的条目先退出正式判定 | 每个开放练习有出处与复核记录；文字、示范、规则一致 |
| 1 修复基础错误 | A01–A09；保留当前页面，不重写整个项目 | 新回归能捕获旧问题；镜像与导出有可复查证据 |
| 2 真人验证 | 已核定小范围：正确动作、典型错误、无法判断 | 来源、条件、独立标签与版本齐全 |
| 3 练习价值 | 顺序、错误复练、本机记录 | 新人能听懂提示并改动作，而不只是画面变绿 |
| 4 算法升级 | 数据迭代与轻量学习对照；动态需求成立后再评估时序模型 | 独立评估优于修复后基线；保留本地运行和回退 |

算法升级的顺序（尚未获准训练）：未滤波 vs 轻量滤波（如 One Euro）→ 跨人标注样本上的随机森林 / SVM 旁路（冲突时不自动通过）→ 仅当动态动作成为瓶颈且已有带起止与错误标签的序列时，再评估小型 TCN。用「收集失败—人工复核—离线调参或训练—独立评估—发布与回退」决定是否替换基线。按人划分数据，相邻帧不跨集合。

当前真正的完成定义：已核定的小范围内容能被可靠判断；不可靠的情况能诚实拒绝；每一个「已修复」都有别人可以复查的证据。
