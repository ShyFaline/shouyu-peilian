# 项目代理执行规则

更新：2026-09-26。适用于本仓库所有执行窗口。先读本文件，再读 `docs/agent-contract.md` 和具体任务。此版本替换旧任务中的“本轮禁止 commit/push”、真人示范立即接入及 GPT 独立验证安排；不改写历史事实，不扩大对外发布权限。

## 当前优先级

1. **真人示范暂缓**：暂停新拍摄、照片/视频处理、真人素材接入和新的素材需求扩张。已产出的清单、候选或未提交改动保留，不删除、不覆盖、不为完成旧 prompt 继续开发。
2. **先跑通整个现有项目**：验证启动、本地资源加载、主路径与图鉴、摄像头启动/失败/停止/恢复、镜像、判定状态、目标切换、有效快照导出、离线回放与现有部署。只修阻断现有流程的问题。
3. 暂停三维手模重建、前端整站重设计、扩字母和训练。现有素材不足不阻塞工程联调，但不能把草稿、未核定内容改称正式标准示范。
4. 项目负责人已确认其余真人验收通过：如实记录为“负责人确认”，关联已有证据；不得冒充本次代理独立复验，不补造参与者、样本或指标。新素材、内容核定和公开使用授权不自动继承该确认。
5. 独立验证席为 **K3 验证窗口**；与 **K3 文书窗口**分离。总控负责最终验收。自测、独立验证、总控验收三者不可混称。

## 每个完整改动都必须 commit 和 push

用户已授权本仓库常规任务分支的提交与推送，不必对每次正常提交重复询问。执行单位为**一个独立、可验证、可回退的逻辑改动**，不是每次保存或每改一行。

1. 开工前记录分支、HEAD、工作区修改和文件所有权；先检查目标文件，保护用户及其他窗口的工作。
2. 每个逻辑改动完成后，运行与改动匹配的测试/检查，再查看 diff；只暂存本席本次明确负责的文件或变更块。
3. 禁止 `git add .`、`git add -A` 打包全工作区。禁止把他人改动、无关文件、临时生成物一并提交。
4. 提交说明写清本次目的。随后正常 push 到当前任务分支的远端；首次用 `git push -u origin <任务分支>`。交付时报告提交哈希、远端分支和实际推送结果。
5. **push 不等于验收、合并或上线。** 在 `main` 上工作时，先在隔离工作副本建立任务分支；不要在多人共用的工作目录里随意切换分支。禁止自动合并到 `main`。当前 `main` 的 `practice/**` 推送会触发 GitHub Pages 部署，须总控验收及负责人明确授权集成后才能执行。
6. 多窗口独立 worktree/工作副本是推荐方案；它们默认不含当前目录的未提交内容。需要这些内容时，先交接明确文件及版本，不得把旧 HEAD 当当前基线，也不得把其他窗口的未完成工作擅自复制进自己的提交。
7. 多窗口共用工作目录时，文件写锁与 Git 索引/提交操作必须互斥。开始和提交前重新检查暂存区；已有他人暂存内容时停止 Git 写操作并报告，不能清空或夹带。不能安全隔离时，将自己的改动交集成席串行提交，不抢锁。
8. 测试失败先定位并修复；不得把失败改动标成通过。必须保存未完成工作时，只能提交到明确标记 WIP 的任务分支，并写明失败及禁合并；涉及他人文件或敏感内容则先停止。
9. 推送被拒、鉴权失败、远端分叉或保护规则阻止时，保留本地提交并报告原始原因。不得 force push、改远端、关保护、自动 reset/rebase、丢弃他人工作或谎称已同步。
10. **推送会向远程公开/传输内容。** 提交前检查密钥、个人信息、真人原片、授权书、私密样本及无再分发许可材料。用户的常规 push 授权不包含这些资料；不上传、不绕过忽略规则。只读验证且没有文件改动时，不制造空提交。

## 五环节与交付

每个板块：总控任务 → 实现 → 文书事实 → 独立验证 → 总控验收。没有必要代码的任务可写“不适用”，不凑工作。

交付至少包含：任务范围、实际改动、基准/提交、实际测试命令与退出码、未执行项、剩余风险、push 状态。测试版本须对应最终文件；自报的失败需区分业务缺陷、测试加载器故障及运行环境问题。

- 核心：`bun practice/src/evaluate.test.js`
- VM 行为：`node --experimental-vm-modules practice/src/reliability.test.js`（核实是真正 Node；Bun 的 VM 加载失败不能直接算产品缺陷）
- 评测：`bun tools/assessment/verify-skeleton.mjs`
- 迁移：`bun tools/assessment/verify-legacy-migration.mjs`
- 本机服务：从仓库根执行 `python -m http.server 8765 --bind 127.0.0.1`；访问 `http://127.0.0.1:8765/practice/index.html`。先查端口，勿重复启动或终止不属于本任务的进程。

浏览器 mock 不是真人；资源在本地不等于已做断网验证；启动成功不等于所有流程通过。发现真实阻断再做最小修复，不以“先跑通”为由关闭状态门、降低阈值或编造素材。

## 已发旧 prompt 的处理

收到本次变更通知后，先读本文件。停止旧任务中真人示范相关的新增工作，交接已完成文件及未提交差异。不要自动撤销已有成果。AGENTS.md 是执行约定，不是自动中断机制；已在运行的其他窗口需要负责人发送变更通知或主动重新读取，不能声称文件一写就已通知所有代理。
