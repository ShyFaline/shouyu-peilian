# 交接（2026-09-21）

接手前读本文件。事实以 GitHub `main` **`8ce6aa0`** 为准。口头「完成了」不算。

仓库：https://github.com/ShyFaline/shouyu-peilian  
路径：`C:\Users\15424\Desktop\2026.9.15`

---

## 1. 产品是什么

浏览器本地 **GF 0021—2019 国标手指字母陪练**。不是翻译器。不是证书。

开摄像头 → 默认 **U** → 左边标准手 → 「到位 / 还不到位」+ 一句 hint → 切 **V**。  
识别在本机。视频不上传。不显示分数。

主路径：`A B U V L Y I W`。其余 24 个在图鉴。M/N/S、E/ê 标易混，不假装能分。

运行：

```
python -m http.server 8765
```

http://127.0.0.1:8765/practice/index.html  
不要 `file://`。桌面 Chrome。

测试：`bun practice/src/evaluate.test.js` → **12 passed**（合成点，不是真人）。

---

## 2. 比赛交件（邮件高于公众号）

预选 **2026-10-23 24:00**。缺一弃赛。

zip：`国标手指字母陪练+队长姓名.zip` → `marketing@newtouch.com`

| 必交 | 现状 |
|---|---|
| **产品说明书**（不要商业计划书）：介绍、思路、过程、价值或创新、AI 工具名与用途 | **未成稿**。底稿：`项目情况简述.md`、`docs/AI-CODING过程.md` |
| **Demo**：网站和/或视频 | 网站能跑。**录屏未做** |

一人一队一项目。行业：教育。队长保持电话畅通，加官方群。

公众号曾要求过程单独成篇。交件以邮件为准：过程写入说明书。`docs/AI-CODING过程.md` 不当 zip 里的唯一过程文件。

说明书禁止：准确率、学会手语、翻译器、合成夹具当真人、渲染图当识别模型。

AI 工具可写：Grok 4.6 规划 / 技术 / 文书；Blender 4.5 出示范图，不进识别循环。

---

## 3. 阶段

| 阶段 | 状态 |
|---|---|
| S0 看见手 | 完成 |
| S1 合成闭环 | 完成 |
| S1.5 自渲图过 Landmarker | 完成。8 张里 6 张无手 |
| B 站抽检 | 完成。实拍多数有手；无字母标签，不报准确率 |
| S2 真人 | **未开始** |
| S3 手图精修 | 未开始。不因 S1.5 去讨好检测器 |

S1.5：`practice/src/render-loop/RESULTS.md`  
A 1手 pass；I 1手 fail；B/U/V/L/Y/W 0手。A pass 不是准确率。

B 站：`practice/src/bili-loop/RESULTS.md`。视频在 `tmp-media/`（gitignore）。U/V 规则同时 pass=0。仓库 U 规则是食中并拢；有的国标讲解 U 是四指并拢。未改 rules。

---

## 4. 目录

| 路径 | 用途 |
|---|---|
| `practice/` | 可运行页 |
| `practice/src/evaluate.js` | 几何判定 |
| `practice/src/evaluate.test.js` | 12 条夹具 |
| `practice/src/eval-handframe.mjs` | 对导出 JSON 打判定 |
| `practice/src/types.js` | HandFrame / Issue / PracticeEvent |
| `practice/src/fixtures/` | **空**。只收真人导出 JSON |
| `practice/content/demos/` | 8 张 `GF0021.*_front.png`，草稿 |
| `practice/sources.html` | 来源 |
| `blender/` | 内容工厂。`hand_gf0021.blend` |
| `docs/AI-CODING过程.md` | 说明书「过程 / 工具」底稿 |
| `docs/来源与伦理草稿.md` | 来源底稿 |
| `docs/试用户记录.md` | 空表 |
| `docs/agent-contract.md` | 写锁 |
| `总体目标与并行拆分.md` | 规划 |
| `项目情况简述.md` | 报名简述，不是说明书 |
| `tmp-media/` | 本机视频，不入库 |
| `blender/hand_gf0021.blend1` | Blender 备份，不入库 |

---

## 5. 工人编制

规划一窗。工人三席 + 只读验收。语气：冷漠。禁止比喻。抢写锁即停。

| 席位 | 写 |
|---|---|
| 技术 | `practice/` 除 `content/demos/*.png` |
| 文书 | `docs/*.md`、`项目情况简述.md` |
| Blender | `blender/`、`practice/content/demos/` |
| 验收 | 零写入 |

JSON 导出已在练习页，默认关。不要重做。不要口播。不要第四个写代码的工人。

---

## 6. 否决（不要回头做）

1. 不做翻译器  
2. 不自训检测网  
3. 预选不做实时跟手虚拟人  
4. 不凑模型席位  
5. 主路径不铺 32 键  
6. 不用 ASL 当国标  
7. 不把未授权录像当开源数据集入库  
8. 不改阈值赶绿、不给 M/N/S 假装能分  

---

## 7. 接手后立刻做

1. **说明书成稿**（五块：介绍、思路、过程、创新、工具）。从简述+过程篇抽，不要新编准确率。  
2. **录屏**：开摄像头 → 比 U → 左标准手 → hint 或到位 → 切 V。  
3. **S2**：一人试 U/V，导出 JSON → `practice/src/fixtures/`，原话 → `docs/试用户记录.md`。  
4. 队长姓名写入简述和 zip 文件名。  
5. 细则若再改，以最新邮件为准，改 `总体目标与并行拆分.md` §0.2。

不要先做 S3。不要先改 U 的 rules。
